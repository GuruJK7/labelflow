# Decisiones — qué, por qué, cómo revertirlo

Registro exigido por la regla 3 del prompt maestro. Donde el prompt choca con lo
que ya está construido y verificado, gana lo verificado y se explica.

## D1 · El monedero se denomina en PLATA, no "1 Coin = 1 guía"
- **Prompt maestro:** 1 Coin = 1 guía; packs con precio fijo; bots con costo en Coins.
- **Decidido:** saldo en milésimos de UYU; el descuento por volumen se aplica **al consumir** (`periodTotalMilli`, monótona), no al comprar.
- **Por qué:** la revisión adversarial del 2026-09-01 mostró que "Coin = guía" + tramos abre dos fugas: (a) con rebates retroactivos, un depósito de 7.000 terminaba en 12.087 de saldo; (b) sin rebates, el sobrante de un pack barato compra bots al mismo precio en Coins que el pack caro → subsidio cruzado con COGS en USD. Con saldo en plata no hay conversión que arbitrar.
- **Cómo se ve para el cliente:** igual que Escalafy — "hacés N envíos por mes → te sale $X/mes". El simulador vive en `quote()`.
- **Revertir:** `tiers.ts` es puro; cambiar a packs fijos es reemplazar `periodTotalMilli` por lookup de pack. Los asientos no cambian.

## D2 · Se conservan los precios vigentes (20/17/15/12/10/7 UYU)
- No se inventan precios: el `[[COMPLETAR]]` de packs quedó vacío y **los precios reales ya existen** en `credit-packs.ts`. Se eliminan las cinco zonas muertas (43–49, 89–99, 201–249, 417–499, 701–999) con la regla "nunca más que el techo del tramo siguiente".
- **Revertir:** quitar el `min` en `periodTotalMilli`.

## D3 · Banco único en LabelFlow (Postgres de `web`), no en Supabase de AE/AB
- **Por qué:** el hecho facturable (la guía DAC) nace en el worker de LabelFlow; con el ledger en otra base hay un salto de red entre "hay guía" y "se cobró" (vuelve F1/F2 disfrazado). 2 de 3 jueces independientes.
- **Revertir:** el core es puro; la persistencia es un adaptador.

## D4 · Auth: NextAuth se queda (no Better Auth)
- Prompt: Better Auth "si no hay nada". Hay NextAuth con Google + email, reset de contraseña y verificación. Extender, no reescribir.

## D5 · Colas: se queda el polling en Postgres (no pg-boss)
- El worker ya reclama jobs con `FOR UPDATE SKIP LOCKED` y lease por tenant (`DacProcessingLease`). pg-boss sería una segunda cola sobre la misma base sin ganancia.

## D6 · Shopify: OAuth público como camino principal; token manual como fallback
- Prompt: camino A (token) principal. Ya existe app pública (`417965080577`, distribución pública, `oauth-inicial` activa) y el flujo App Store (PR #3). El token manual queda plegado en Settings porque hay tenants vivos conectados así.

## D7 · Trial: 5 por tienda, atado a `shopDomain` (unique) — pero HOY son 10 por cuenta
- `Tenant.shipmentCredits @default(10)` y `REFEREE_BONUS_CREDITS=10`. Bajar a 5 es un cambio de política comercial: **se documenta, no se aplica** hasta que Adrian lo confirme (los clientes vivos ya vieron "10").

## D8 · Whop = segundo riel, nunca reemplazo de MercadoPago
- Ver `docs/PAGOS.md`. 2,7 % + USD 0,30 fijo: ~20 % efectivo sobre un ticket de USD 2. No emite CFE/DGI.

## D9 · Migraciones: se escriben y se prueban, NO se aplican a prod en el mismo turno
- CLAUDE.md: prod es solo lectura; lo irreversible se marca 🔴 y se ejecuta en otro turno con OK.

## D10 · `app/uninstalled` no toca `isActive`
- `isActive` es el flag de facturación que lee el scheduler; apagarlo cortaba todas las fuentes del cliente y nada lo revertía. Se limpia sólo el token.

## D11 · Identidad en el App Store: `shop.email` NO es identidad; una cuenta existente RECLAMA la tienda logueada
- **Antes (PR #3 v1):** `provisionFromShopify` hacía upsert de User por el email de contacto de la tienda. Si ese email ya era cliente, la tienda se le colgaba sola. Ese email lo edita el comerciante y Shopify no lo verifica: cualquiera podía meter una tienda ajena en la cuenta de un cliente, o la tienda del comerciante quedaba bajo la cuenta de la agencia que le administra el admin.
- **Decidido:** tienda nueva + email sin cuenta → se crea User + Tenant (`created`). Tienda nueva + email CON cuenta → no se escribe nada (`claim`): el token viaja cifrado (misma primitiva AES-256-GCM que `shopifyToken`) en la cookie `shopify_pending_install` (httpOnly, 600 s, path `/api/shopify`) y el dueño la reclama en `GET /api/shopify/claim` con sesión, dentro de una transacción que re-verifica que la tienda siga libre. Tienda ya vinculada al User de ese email → `existing` (refresca token). Vinculada a otro → `conflict`.
- **Por qué cookie y no tabla:** D9 — nada de migraciones a prod en este turno. La cookie cifrada+autenticada da lo mismo con TTL gratis y sin token suelto en la base.
- **Revertir:** si algún día se agrega tabla `PendingShopifyInstall`, `sealPendingInstall/openPendingInstall` se reemplazan por insert/select; el resto del flujo no cambia.

## D12 · El mail de "elegí tu contraseña" sale SÓLO en `created`; `/entry` no reinicia OAuth si la tienda ya está conectada
- **Antes:** el callback mandaba `issueAndSendPasswordResetEmail` en `created` y en `existing`, y como Shopify carga la App URL en cada apertura desde el admin, cada apertura borraba los tokens de reset vigentes y mandaba otro mail (saltando el rate limit de 5/h del endpoint).
- **Decidido:** mail sólo con `alta.kind === 'created'`. En `/entry`, si existe Tenant con `shopifyStoreUrl == shop` y `shopifyToken` no nulo → `/login?shopify=open` sin pasar por `authorize`. Una tienda desinstalada (token en null por `app/uninstalled`) sí vuelve a instalar.
- **Etiqueta:** que Shopify pegue a la App URL en cada apertura es conocimiento estable de la plataforma, no reconfirmado hoy en shopify.dev. Si fuera falso, D12 sigue siendo correcto (reinstalar tampoco debe mandar un reset).

## D13 · Orden de la rama dashboard, higiene de cookies y destinos públicos
- **Rama B (dashboard):** sesión → propiedad → `shop_mismatch` → `already_linked` **antes** del canje del `code` (como en main). Un `code` canjeado emite un token offline vivo; nada que pueda fallar por permisos corre después del canje. La rama A canjea primero porque sin token no se puede preguntar de quién es la tienda.
- **Cookies:** `/install` borra `FLOW_COOKIE`; `/entry` borra `TENANT_COOKIE`; el callback rechaza `FLOW=appstore` + `TENANT` a la vez con `bad_flow`. Sin esto, un "Instalar" del App Store abandonado secuestraba el "Conectar" del dashboard dentro de los 10 minutos.
- **Destinos:** en flujo App Store todo (éxito, `fail()`, `missing_scopes`) aterriza en `/login?shopify=<motivo>`; nunca en `/settings` (rebota sin sesión y pierde el motivo). El email nunca va en la query. Los textos viven en `lib/shopify-messages.ts` (compartido por `/settings` y `/login`). `LoginForm` honra `?next=`/`?callbackUrl=` sólo si es ruta relativa (`safeRelativePath`): sin eso, `/login` sería un open redirect.

## D14 · Tenant aprovisionado desde Shopify: `apiKey` aleatoria, `referralCode`, `tosAcceptedAt` en null, slug con hash
- `apiKey = randomBytes(32).hex` y `referralCode` como en signup, vía `lib/tenant-provision.ts` (helper compartido; signup lo usa también). `cuid()` no es criptográficamente aleatorio y `apiKey` es credencial de la API pública.
- `tosAcceptedAt` queda **null**: el comerciante autorizó la app en Shopify, no aceptó nuestros términos. Verificado por grep que ningún gate lo lee (sólo se setea en signup, `/api/v1/tenants`, `dac-tenant` y auth Google). La aceptación en el primer login está en PENDIENTES.md.
- Slug: handle ≤ 40 → `shop-<handle>`; > 40 → `shop-` + 31 primeros + `-` + sha256(handle)[0:8]. Antes se truncaba a 40 y dos tiendas con el mismo prefijo colisionaban en `shop_taken`. `existingByShop` y el insert van en la misma transacción; `P2002` → `conflict`.

## D15 · El cutover a `authoritative=true` se hace SÓLO el día 1 del mes (hora Uruguay)
- **Problema:** `readPeriod` suma `shipment + settlement + refund` del período sin
  mirar `shadow`. Si un user se flipea a mitad de mes con 40 envíos ya asentados en
  sombra (y cobrados en créditos por el contador), el envío 41 se liquida contra
  `n = 41` y el único delta que toca `balanceMilli` es el marginal: el descuento por
  volumen lo "ganaron" envíos que se pagaron por otro sistema. No es un bug del
  ledger — el invariante cierra igual — pero es una decisión de precio implícita y
  el balance del mes de cutover queda ilegible.
- **Decidido:** el flip `Wallet.authoritative=true` + `cutoverAt` se hace el **día 1
  del mes calendario en `America/Montevideo`** (UTC-3 fijo, `periodOf`), antes del
  primer envío de ese mes, con el despacho de todos los tenants del user pausado.
  Así el período del cutover arranca en `n = 0` y todo su neto es real.
- **Descartado:** emitir un `adjust` que deje el mes con `neto = -periodTotal(n_sombra)`
  al flipear. Funciona, pero mete un asiento sintético que el reconciliador tiene que
  saber distinguir para siempre. Regla de calendario > asiento especial.
- **Consecuencia operativa:** si un cutover se pierde el día 1, se espera al mes
  siguiente. No se flipea el 2.
- **Revertir:** si algún día hace falta cortar a mitad de mes, la alternativa
  descartada (el `adjust`) es la que hay que implementar y documentar; no
  alcanza con flipear el flag.

## D16 · `/entry` no distingue token OAuth de token manual: el cortocircuito se queda, la migración es por Reconectar
- **Hallazgo (re-revisión de seguridad, bajo):** `/api/shopify/entry` corta el OAuth si el tenant ya tiene `shopifyToken` (D12), también cuando ese token lo pegó a mano un cliente viejo desde su custom app. Para ese cliente, "Instalar" desde el App Store termina en `/login?shopify=open` y Shopify nunca registra la instalación pública.
- **Decidido:** no se cambia el comportamiento. Distinguir el origen del token exige una columna nueva (`shopifyTokenSource` o similar) y eso es migración a prod: D9 lo prohíbe en el mismo turno. Reiniciar OAuth siempre que la tienda ya esté conectada volvería a abrir lo que D12 cerró (aprovisionar y mandar mail en cada apertura desde el admin).
- **Camino de migración:** Settings → **Reconectar**, que sí pasa por `/install` → OAuth → `/callback` rama B y reemplaza el token manual por uno público. Está en PENDIENTES.md como tarea por cliente.
- **Revertir:** cuando exista la columna de origen, `/entry` pasa a cortocircuitar sólo con `shopifyTokenSource = 'oauth'`; el test "tienda ya vinculada con token vigente" cambia el `where`.

## D17 · Reclamar una tienda no la deja activa: el banner de /settings la nombra
- El tenant activo vive en el JWT y sólo cambia con `POST /api/v1/tenants/switch` + `useSession().update({ tenantId })` desde el cliente (`TenantSwitcher`). Un redirect de `/api/shopify/claim` no puede hacer eso, y no se inventa un mecanismo nuevo por URL para un caso de borde.
- `/claim` redirige a `/settings?shopify=connected&shop=<handle>`; Settings valida el handle (`shopHandleFromParam`, misma forma que `normalizeShopDomain`) y muestra "La tienda <handle> quedó conectada como tienda nueva: elegila en el selector…". Lo que no pasa la validación no se muestra: el banner verde de "conectada" es el peor lugar para texto que venga de la URL.
- **Otros ajustes del mismo ciclo:** el tenant reclamado nace con `shipmentCredits = 0` (el bonus es por usuario, igual que `POST /api/v1/tenants`); `/callback` en `claim` va directo a `/claim` (si hay sesión reclama en el acto; sin sesión `/claim` manda a `/login?shopify=claim&next=…`); la cookie pendiente se cifra con el prefijo `pending-install:v1:` para que un `shopifyToken` cifrado con la misma clave no abra como cookie; `already_linked` tiene texto propio en el login.

## D18 · `shopifyStoreUrl` se compara y se guarda en minúsculas; `UPDATE … lower()` escrito, no aplicado (el índice único es D22)
- **Hallazgo (revisión de seguridad, medio):** el camino manual (`PUT /api/v1/settings`, onboarding) aceptaba `MiTienda.myshopify.com` y lo guardaba tal cual; todo el flujo del App Store compara con el dominio normalizado en minúsculas. Para ese cliente, "Instalar" desde el App Store no lo encontraba: `/entry` arrancaba OAuth y `provisionFromShopify` creaba una segunda cuenta o un segundo tenant para la misma tienda → el scheduler despachaba cada pedido dos veces. El webhook (Shopify manda el dominio en minúsculas) sólo le pegaba al nuevo.
- **Decidido (código, sin migración):** (a) los dos zod hacen `.transform((s) => s.toLowerCase())` después del regex: ningún camino nuevo guarda mayúsculas. (b) Búsquedas por dominio con `{ equals: shop, mode: 'insensitive' }` (Prisma sobre Postgres), lista EXACTA en `apps/web`: `/entry`, `/install`, `/callback` rama B (`tomadaPorOtro`), `provisionFromShopify` (`existingByShop`), `/claim` (`tomada`, en la transacción), `settings PUT` (chequeo de duplicados, D21), el webhook `api/webhooks/shopify` (`findFirst`), `api/webhooks/shopify/gdpr` (`findFirst`), `api/webhooks/shopify/checkouts` (`findFirst`, junto con `isActive`) y `api/shopify/uninstalled` (`updateMany` que limpia el token). Las tres últimas habían quedado exactas en la primera pasada y se corrigieron en la última revisión, con un test cada una (`shopify-{uninstalled,gdpr,checkouts}-route.test.ts`: una fila `MiTienda.myshopify.com` se encuentra con `mitienda.myshopify.com`). Lo que este PR NO cubre: el worker (`apps/worker/src/jobs/process-orders.job.ts`, `sharedTenantIds`) compara `shopifyStoreUrl` EXACTO; para ese caso sólo el `UPDATE … lower()` de la migración sirve. `shop_mismatch` en el callback compara en minúsculas: Reconectar es el camino de migración de los tokens manuales (D16) y un dominio viejo con mayúsculas no puede bloquearlo. Los tests prueban la propiedad con una tabla en memoria que evalúa el `where` (`fakeTenantFindFirst`), no sólo la forma del objeto.
- **Decidido (base, D9):** `apps/web/prisma/migrations/20260901180000_tenant_shop_domain_lower/migration.sql` con SÓLO el `UPDATE "Tenant" SET "shopifyStoreUrl" = lower(…) WHERE … <> lower(…)`, envuelto en `BEGIN/COMMIT`, con el SELECT de duplicados por `lower()` y el SELECT de filas afectadas en el encabezado como chequeo previo. El índice único parcial se separó a `20260901180100_tenant_shop_domain_unique` porque es otra decisión (D22). 🔴 Sin aplicar; el repo no usa `prisma migrate` (la carpeta no existía), se corre a mano sobre `DIRECT_URL`. `schema.prisma` no cambia. El UPDATE hace falta aunque el código ya compare insensible: el worker (`sharedTenantIds`) compara exacto, ver (b).
- **Lo que sigue sin cubrir hasta aplicar el índice:** la carrera reclamo-vs-"Conectar del dashboard" (rama B chequea `tomadaPorOtro` fuera de transacción). Ventana de milisegundos, requiere dos flujos a la vez sobre la misma tienda.
- **Revertir:** el UPDATE no se revierte (no queda registro de qué filas tenían mayúsculas, y ninguna las necesita: Shopify no distingue). El índice, si llegara a aplicarse, con `DROP INDEX "Tenant_shopifyStoreUrl_key"` (D22).

## D19 · `/claim` pregunta en el GET y escribe en el POST; sin token CSRF propio porque la cookie es `lax`
- **Hallazgo (bajo):** desde d2b9968 el callback mandaba directo a `/claim`, y `/claim` con sesión escribía en el acto. Eso ataba el token offline de la tienda a la sesión que hubiera en el navegador: en una máquina compartida (agencia que "ayuda a instalar", empleado, dueño anterior) la tienda quedaba bajo la cuenta equivocada y el dueño real recibía `already_linked` para siempre. Es el escenario de D11 reabierto por sesión en vez de por email.
- **Decidido:** `GET /api/shopify/claim` con sesión y cookie válida devuelve una página HTML mínima (server-rendered desde la ruta, sin JS externo, estilos inline) que dice "¿Vincular `<shop>` a la cuenta `<email>`?" con `<form method="post" action="/api/shopify/claim">` y un enlace "Entrar con otra cuenta" a `/api/auth/signout?callbackUrl=<encoded /login?shopify=claim&next=/api/shopify/claim>` (NextAuth 4.24: el GET de signout guarda `callbackUrl` en su cookie y el POST del botón "Sign out" redirige ahí; verificado en `core/init.js` + `core/lib/callback-url.js`). `POST` hace lo que hacía el GET: sesión + cookie + transacción + webhooks + borrar cookie + redirect. GET sin sesión sigue mandando a `/login?shopify=claim&next=/api/shopify/claim` con la cookie intacta. Shop y email pasan por `escapeHtml` (`lib/html-escape.ts`).
- **Por qué no hace falta token anti-CSRF:** la cookie pendiente es `sameSite=lax` con `path=/api/shopify`. Un navegador no adjunta una cookie lax a un POST iniciado desde otro sitio (lax sólo la manda en navegaciones top-level con método seguro), así que un formulario ajeno que apunte al POST llega sin cookie → `claim_expired`, sin tocar la base. La cookie de sesión de NextAuth es lax también, así que ese POST llega además sin sesión. El único POST que trae las dos es el del formulario que servimos nosotros.
- **Confirmación ligada a lo mostrado (última pasada):** el formulario del GET lleva `<input type="hidden" name="shop" value="<shop escapado>">` con la misma tienda del título, y el POST, después de abrir la cookie, exige `form.get('shop') === shop` o devuelve `claim_invalid` (cookie borrada, nada escrito). Ata el clic a lo que la persona leyó: si la cookie cambió entre el GET y el POST (otra instalación en otra pestaña dentro de los 10 minutos), no se vincula una tienda que nadie confirmó. Tests: cookie sellada con B + form `shop=A` → `claim_invalid` sin `create`; POST sin cuerpo → `claim_invalid`.
- **`Origin` como segunda cerradura (última pasada):** si el POST trae header `Origin` y no es `req.nextUrl.origin`, `claim_invalid` ANTES de leer sesión o cookie (cookie borrada, base intacta). Sin header se sigue: clientes viejos y algunos modos privados no lo mandan, y la cookie lax sola ya alcanza. Cuesta una línea y no depende de que todos los navegadores respeten `lax` bien. Tests: `Origin: https://evil.example` con cookies válidas → `claim_invalid` sin llamar a `getAuthenticatedUser`; `Origin` propio → `connected`.
- **Post/Redirect/Get (última pasada):** todos los redirects que salen del POST (login, `claim_*`, `already_*`, `connected`) van con **303**. `NextResponse.redirect` usa 307 por defecto, que conserva el método: el navegador seguía el redirect con OTRO POST a `/settings`, y un refresh en `/settings` re-enviaba el formulario. El GET conserva el 307 (no viene de un formulario). Tests: `expect(res.status).toBe(303)` en cada salida del POST y `307` en el login del GET.
- **Revertir:** si se quisiera exigir re-autenticación real, el botón "Vincular" pasa a ir a `/api/auth/signout?callbackUrl=…` siempre; el POST no cambia.

## D20 · Log de `/claim` sin valores de fila; `already_yours` cuando la tienda ya es del mismo usuario
- **Log:** `console.error('[shopify/claim]', { shop, userId, name, code, message })` con `message` entero SÓLO si el error es `Prisma.PrismaClientKnownRequestError` (P-xxxx: nunca trae valores de fila); para cualquier otro (p.ej. `PrismaClientValidationError`, que vuelca los args del `create` con el token cifrado y la apiKey) va la primera línea de `String(message)` recortada a 200 chars. El test rechaza con un `Error` cuyo message contiene `shpat_pend_XXX` y el valor de la cookie, y afirma que el log no los contiene: prueba la propiedad, no la forma.
- **`already_yours`:** el `findFirst` de la transacción selecciona `userId`; si la tienda ya es de un tenant del usuario de la sesión (la conectó por Reconectar o con token manual mientras la cookie vivía), `/claim` redirige a `/settings?shopify=already_yours&shop=<handle>` y Settings muestra "La tienda <handle> ya está conectada a tu cuenta: elegila en el selector." (sin handle válido, el texto genérico del diccionario). Antes decía "conectada a OTRA cuenta, escribinos": un ticket de soporte por cada caso. No aplica al login: `/claim` siempre exige sesión.
- **Comentario corregido:** `/claim` y `provisionFromShopify` decían "índice único" hablando de `shopifyStoreUrl`; el único índice es el del `slug` (determinista, `shop-<handle>`), y el `findFirst` en transacción es la otra mitad de la protección. Ahora lo dicen así, y apuntan a D18 por el índice real. Además, `settings PUT` hace el mismo `findFirst({ shopifyStoreUrl insensitive, id: { not: tenantId } })` → 409 que ya hacía `/install`.

## D21 · `settings PUT` sólo chequea duplicados de tienda cuando el dominio CAMBIA
- **Hallazgo (revisión de seguridad, alto):** el chequeo de D20 (`findFirst({ shopifyStoreUrl insensitive, id: { not: tenantId } })` → 409) saltaba siempre que el body trajera `shopifyStoreUrl`, y "Guardar token" de `/settings` lo manda SIEMPRE con el valor que cargó del GET. Un tenant que comparte tienda con otro no podía rotar ni pegar un token nuevo: 409 "conectada a otra cuenta" sin haber tocado el dominio. Compartir tienda no es hipotético ni ilegítimo: `94a6282` (incidente Aura 2026-05-08) documenta a "Alex" y "Nueva tienda" sobre la misma tienda y el worker lo contempla con `sharedTenantIds` (`process-orders.job.ts`). Y `/install` y `/callback` ya les devolvían `already_linked` desde main: el token manual era el único camino que les quedaba y el PR lo cerraba.
- **Decidido:** antes del `findFirst`, `findUnique({ where: { id: tenantId }, select: { shopifyStoreUrl } })`; si `actual.toLowerCase() === input` (el zod ya bajó el input a minúsculas, D18) se salta el chequeo de duplicados y se sigue con el UPDATE (que normaliza el dominio viejo a minúsculas). Si el dominio cambia, el chequeo es el de D20. La UI no cambia: seguir mandando el dominio junto al token es lo que dispara la verificación del token contra Shopify (`shop.json`) antes de guardarlo; mandarlo sólo "si difiere" habría guardado tokens sin verificar.
- **Tests:** `settings-route-shop-domain.test.ts`: tienda compartida (tenant-1 `MiTienda.myshopify.com`, tenant-ajeno `mitienda.myshopify.com`) + PUT con el mismo dominio y token → 200, sin consultar duplicados, `shop.json` consultado, dominio en minúsculas y token cifrado; el mismo tenant cambiando a un dominio de un tercero → 409; a uno libre → 200 con consulta.
- **Tensión con D18, resuelta en D22:** el índice único parcial se separó a su propia migración (`20260901180100_tenant_shop_domain_unique`) y quedó como decisión pendiente de Adrian: aplicarlo implica retirar el diseño de tienda compartida. El `UPDATE … lower()` no depende de esa decisión.
- **Revertir:** borrar el `findUnique` y el `sinCambio`; el 409 vuelve a saltar en cada "Guardar token".

## D22 · El índice único sobre `shopifyStoreUrl` es decisión de Adrian, no del código: migración separada, no aplicar sin esa decisión
- **Hallazgo (última pasada):** el archivo de D18 mezclaba dos cosas de distinto peso: un `UPDATE … lower()` que sólo deja la base coherente con lo que el código ya escribe, y un `CREATE UNIQUE INDEX` que cambia el modelo. `main` soporta a propósito que dos tenants compartan tienda: `94a6282` (incidente Aura 2026-05-08, near-miss: "Nueva tienda" estuvo a un ciclo de re-despachar y re-facturar en DAC los pedidos #1147–#1158 que "Alex" ya había despachado; el operador pausó a los dos antes) lo arregló haciendo que el worker lo contemple (`sharedTenantIds` en `process-orders.job.ts`), no prohibiéndolo, y el propio commit lo declara legítimo (distintos horarios, filtros de producto, auditoría durante una reinstalación) aunque recomiende un tenant por tienda. `settings PUT` lo respeta desde D21.
- **Decidido:** dos archivos. (a) `20260901180000_tenant_shop_domain_lower`: SÓLO el UPDATE, en `BEGIN/COMMIT`, con los SELECT de chequeo en el encabezado; no requiere decidir nada (D9: en otro turno, a mano, sobre `DIRECT_URL`). (b) `20260901180100_tenant_shop_domain_unique`: el índice único parcial, con encabezado que dice que aplicarlo implica retirar el diseño de tienda compartida (los tenants que hoy la comparten quedan con `shopifyStoreUrl NULL` salvo uno, saldo y etiquetas revisados a mano, `sharedTenantIds` pasa a ser código muerto) y qué se gana (cierra la carrera reclamo-vs-Conectar del dashboard; el P2002 que /claim, /callback y `provisionFromShopify` ya traducen salta también por dominio, no sólo por slug). 🔴 NO aplicar (b) sin decisión explícita de Adrian, y sólo después de (a) y con el SELECT de duplicados en 0.
- **Lo que queda abierto mientras no se decida:** la carrera reclamo-vs-"Conectar del dashboard" de D18 (ventana de milisegundos, dos flujos a la vez sobre la misma tienda).
- **Revertir:** (b) con `DROP INDEX "Tenant_shopifyStoreUrl_key"`; (a) no se revierte ni hace falta.

## D23 · Vuelve el alta pública (self-serve); `ALLOW_PUBLIC_SIGNUP` sigue mandando (default apagada)
- **Decisión de Adrian, 2026-09-01:** "deberías poder crearte una cuenta a la perfección". `/signup` renderiza `SignupForm` otra vez (el form nunca se borró en el pivot B2B de mayo; sólo la página lo tapaba con "Acceso por invitación"). La landing y el login llevan al alta ("Crear cuenta gratis"); WhatsApp queda como "Solicitar demo" (secundario en nav, hero y cierre) y como "¿Preferís que te lo implementemos nosotros?" debajo del form.
- **La bandera manda en los dos caminos:** `POST /api/auth/signup` responde 403 y el alta nueva por Google (`lib/auth.ts` signIn) devuelve `false` si `ALLOW_PUBLIC_SIGNUP !== 'true'`. Default apagada: hasta que el orquestador la ponga en Vercel, el form se ve y el POST contesta 403 con un texto que el form muestra ("El registro público está cerrado por ahora. Escribinos por WhatsApp…"). No se agregó ningún gate nuevo ni se quitó ninguno.
- **Qué NO cambió (y hay que saberlo antes de prometer el alta):** el tenant nace `isActive=false` con `shipmentCredits=10` (default del schema) y sólo `POST /api/v1/onboarding/complete` lo activa, que exige credenciales de Shopify **y** de DAC. `(dashboard)/layout.tsx` redirige a `/onboarding` hasta entonces. Para quien no tiene cuenta DAC, el self-serve termina en `/onboarding` — decisión de producto, en PENDIENTES.md.
- **Copy:** se sacó "valor $200 UYU" del form porque no hay dónde verificarlo en el código; "10 envíos gratis" coincide con `Tenant.shipmentCredits @default(10)`. Metadata de `/signup`: "Creá tu cuenta — AutoEnvía". El logo del form, `/login`, `/verify-email` y el mail siguen diciendo LabelFlow (marca interna): unificar es decisión de marca, no de este turno.
- **Revertir:** `git show 698f2fb:apps/web/app/(auth)/signup/page.tsx` es la página de invitación; los CTAs de `app/page.tsx` y la línea de `LoginForm.tsx` vuelven a WhatsApp/"Solicitar acceso". La bandera no hace falta tocarla.

## D24 · El login con contraseña no exige email verificado ni tenant activo — se deja como está
- **Hecho verificado:** `lib/auth.ts` `authorize` (l.27-70) sólo busca el usuario por email, exige `passwordHash` y compara con bcrypt. No mira `emailVerified` ni `isActive`; el JWT carga `isActive`/`subscriptionStatus` del tenant "holder" como dato informativo. El único gate de verificación es `EMAIL_VERIFICATION_REQUIRED` ('1'/'true') en `(dashboard)/layout.tsx` y `onboarding/layout.tsx` → redirect a `/verify-email?email=`. Apagada por default.
- **No se cambia en este turno:** prender el gate sin `RESEND_API_KEY` viva encierra a todos los nuevos en `/verify-email` sin mail (`sendSystemEmail` soft-fail `no_api_key`), y tocar `authorize` afecta a clientes existentes que nunca verificaron (los aprovisionados por el operador o por Shopify no pasan por el mail de verificación).
- **Verify-email, tal cual existe:** el alta llama a `issueAndSendVerificationEmail` (token 32 bytes, sha256 en la base, TTL 24 h) y manda con `renderVerificationEmail` desde `RESEND_FROM` o `"LabelFlow <noreply@autoenvia.com>"`, tag `verify_email`. `/verify-email` explica el paso, dice "revisá spam", reenvía vía `POST /api/auth/verify-email/send` (rate limit Redis 3/h por email, fail-open, respuesta constante `{ok:true}`) con cooldown de 60 s en el botón. Nada de esto se inventó ni se modificó; sólo se verificó leyendo el código — no se mandó un mail real desde acá.
- **Camino para exigir email real:** en Vercel, primero `RESEND_API_KEY` (y `RESEND_FROM` si el remitente cambia), después `EMAIL_VERIFICATION_REQUIRED=true`, y un alta de prueba que reciba el mail antes de dar por prendido el gate.

## D25 · Anti-abuso mínimo del alta: rate limit por IP en Redis, honeypot, email normalizado
- **Rate limit:** clave `signup:rl:ip:<ip>`, `INCR` + `EXPIRE` 1 h, máximo 5 altas por IP por hora. Es el mismo bloque que `password-reset/request` (5/h por email) y `verify-email/send` (3/h por email). Redis es Upstash (`REDIS_URL`), compartido por todas las lambdas de Vercel: **no es en memoria ni por instancia**. Fail-open: sin `REDIS_URL` o con Redis caído no se limita (preferimos un alta de más a bloquear a un cliente real). IP = primer salto de `x-forwarded-for` (lo escribe Vercel) o `x-real-ip`; sin ninguno, `'unknown'`, y todos los sin-IP comparten un contador.
- **Honeypot:** input `website` fuera de la vista y del tab order (posición absoluta fuera de pantalla, `tabIndex=-1`, `aria-hidden`; no `display:none`, que algunos bots detectan). El form lo manda siempre (vacío). Con valor, el server responde sin tocar la base ni mandar mail; se chequea antes de zod y del rate limit. (Respondía `200 {data:{ok:true}}`, que NO era la forma de un alta real: desde D26 responde `201` con ids falsos, ver ahí.)
- **Email:** `trim()` + `toLowerCase()` antes de validar; un espacio interno no se arregla, se rechaza (400). Lo que se guarda es lo que se busca en el 409 de duplicado y lo que compara el login (`LoginForm` ya bajaba a minúsculas). El 409 ahora dice qué hacer ("Iniciá sesión o recuperá tu contraseña"). Dos POST simultáneos con el mismo email: el segundo pega en el índice único (`P2002`) → 409, antes 500.
- **`email_verification_sent`** sólo se dispara si Resend confirmó (`send.ok`); antes bastaba con que el helper no tirara, y el helper nunca tira.
- **Lo que NO hay:** captcha/Turnstile, bloqueo de dominios desechables, verificación del mailbox antes de crear, rate limit en `POST /api/referrals/track`. (Tope global y agrupación IPv6: ver D26.) Costo residual de abuso: una fila `User` + `Tenant` con 10 créditos por alta y un mail por Resend. El tenant nace inactivo, pero **ojo**: `onboarding/complete` exige credenciales de Shopify (probadas contra la tienda) y de DAC **sin probar** — `test-dac` guarda cualquier string a propósito (el portal de DAC sólo se loguea con Playwright en el worker). Corregido en D26: además de las credenciales, activar exige email verificado. Cuándo sumar Turnstile es decisión del dueño según el volumen de altas falsas después de abrir.
- **Revertir:** borrar `checkSignupRateLimit` y el bloque del honeypot en `route.ts`; los tests de `lib/__tests__/signup-route.test.ts` que los cubren se caen solos.

## D26 · Revisión del alta: tope global de altas, IPv6 por /64, fail-open logueado, y activar exige email verificado
- **Por qué:** la revisión de `feat/signup-publico` mostró dos caminos por script con la bandera prendida. (1) Rotando direcciones, 5/h por IP no limita nada — y en IPv6 un solo /64 da direcciones infinitas: cada alta es User + Tenant + 10 créditos + un mail por Resend, y a 10.000 mails el remitente `noreply@autoenvia.com` pasa la cuota y los mails de verificación y de reset de clientes reales dejan de salir. (2) Ningún gate de despacho miraba `emailVerified`: signup → login (no lo exige, D24) → `test-shopify` con una dev store gratis → `test-dac` con `x`/`x` → `complete` prende `isActive` → el scheduler del worker encola un job cada 15 min por tenant activo con saldo (`scheduler.ts:248`, `cronSchedule` default `*/15 * * * *`), que intenta `smartLogin` contra DAC vía el bridge del Mac mini y falla; los créditos sólo bajan por envíos exitosos, así que el ciclo no termina.
- **Tope global (`apps/web/app/api/auth/signup/route.ts`):** segunda clave `signup:rl:global`, `INCR` + `EXPIRE` 1 h, máximo **40 altas/h en total** → 429 "Estamos recibiendo muchas altas en este momento…". Acota el peor caso a ~1.000 altas/día pase lo que pase con las IPs. Sólo se incrementa cuando la IP pasó su propio límite (dos pipelines, no una): así una sola IP bloqueada no puede agotar el tope de todos. Costo aceptado: un atacante que rota IPs puede llenar el tope y frenar altas reales durante una hora — es el kill-switch barato; la respuesta correcta a ese escenario es Turnstile (PENDIENTES.md).
- **IPv6 por /64 (`apps/web/lib/rate-limit-ip.ts`, `rateLimitBucketForIp`):** si la IP tiene `:`, la clave se arma con los primeros 4 hextetos con `::` expandido (`2001:db8:85a3:1::/64`); `::ffff:a.b.c.d` se trata como la IPv4. IPv4 sigue exacta. Vive en `lib/` porque Next no admite exports que no sean handlers en un `route.ts`.
- **Fail-open logueado:** sin `REDIS_URL` o con Redis caído sigue sin limitar (misma decisión que D25), pero ahora `console.warn('[signup] rate limit fail-open…')` con el mensaje del error (sin IP ni email) — un Redis ausente en Vercel ya no deja el alta sin límite en silencio.
- **Activar exige email verificado (`apps/web/app/api/v1/onboarding/complete/route.ts`):** el select trae `user { email, emailVerified }` y, después de los chequeos de Shopify y DAC, si `emailVerified` es null responde `422 { error: 'Confirmá tu email antes de activar la cuenta…', code: 'email_not_verified', email }` y **no** toca el tenant. Es independiente de `EMAIL_VERIFICATION_REQUIRED` (que sigue apagada y sólo gobierna los layouts, D24): `complete` es la única puerta que prende `isActive`, así que con esto el cron y el botón manual exigen un mailbox real. Los tenants ya completos siguen entrando por el early return (`alreadyComplete`), y los aprovisionados por el operador (`/api/provisioning/dac-tenant`) nacen con `onboardingComplete: true`, así que no los toca. El wizard (`app/onboarding/page.tsx`) muestra el error con un link "Reenviar el mail de confirmación" a `/verify-email?email=` (la página no está en `publicPaths` del middleware, pero el usuario del wizard tiene sesión y pasa; el reenvío `POST /api/auth/verify-email/send` sí es público y tiene su propio rate limit 3/h por email).
- **Email verificado al restablecer la contraseña (`apps/web/app/api/auth/password-reset/confirm/route.ts`, después del rebase sobre PR #3):** el link de reset llega por mail y usarlo demuestra control del buzón, así que el `db.user.update` de la transacción pasa a `{ passwordHash, emailVerified: emailVerified ?? new Date() }` (`findUserByResetToken` devuelve el estado actual; una fecha previa se conserva). Es lo que resuelve al comerciante que instala desde el App Store de Shopify: `provisionFromShopify` no setea `emailVerified` y no se toca (territorio de PR #3), pero el mail "elegí tu contraseña" que le manda `/callback` en `created` (D12) es exactamente este token, así que al elegir contraseña queda verificado y `onboarding/complete` lo deja activar. Vale también para cualquier "olvidé mi contraseña". Quien entra por Google ya venía verificado desde `auth.ts`.
- **Segunda pasada de la revisión de abuso (hallazgos restantes):** (a) `POST /api/auth/verify-email/send` suma dos topes al 3/h por email: 5/día por email (`verify-email:rl:day:<email>`, TTL 24 h) y 20/h por IP (`verify-email:rl:ip:<cubo>`, IPv6 por /64 con `rateLimitBucketForIp`; se chequea primero y una red bloqueada no gasta la cuota del email). Registrar un email ajeno dejaba reenviar hasta 72 mails/día a esa casilla desde `noreply@autoenvia.com`; ahora 5. Fail-open logueado como en el alta. (b) El honeypot del alta responde `201 { data: { userId, tenantId } }` con ids al azar con forma de cuid (`c` + 24 `[a-z0-9]`) en vez de `200 {ok:true}`: la respuesta era distinta de la de un alta real y el bot aprendía a omitir el campo con una sola prueba. (c) Auto-referido: si `referrer.signupIp` coincide con el `signupIp` del alta (las dos presentes y distintas de `unknown`), la atribución `referredById` se conserva (el kickback del 20% sólo se acredita en compra PAGA y va protegido por `userId`) pero `referralBonusCredits` queda en 0. `getRequestIp` pasó a `lib/rate-limit-ip.ts` para que el alta y el reenvío lean la IP igual. Lo que sigue sin haber: Turnstile, dominios desechables, rate limit en `POST /api/referrals/track` (PENDIENTES.md).
- **Lo que sigue faltando (fuera de `apps/web`):** el circuit breaker en el worker — N fallos seguidos de `smartLogin` para un tenant → `isActive=false` + aviso — que es lo que corta el ciclo para una cuenta que sí verificó el mail y cargó un DAC inválido. `apps/worker/**` no se toca en esta rama.
- **Tests:** `lib/__tests__/signup-route.test.ts` (Redis falso con contador por clave: IP bloqueada no consume el global, /64 compartido, la 41 global → 429 y la 40 pasa, fail-open con `console.warn`), `lib/__tests__/rate-limit-ip.test.ts` (IPv4, `::` al principio/medio, IPv4 mapeada), `lib/__tests__/onboarding-complete-route.test.ts` (401, activa con todo, 422 sin email verificado y sin tocar la base, independiente de la env, credenciales antes que email, `alreadyComplete` sin verificar). Segunda pasada: `signup-route.test.ts` (honeypot → 201 con ids con forma de cuid, distintos entre sí y sin tocar la base; referido desde otra IP con bono, misma IP sin bono pero con atribución, referidor sin IP y `unknown` con bono), `verify-email-send-route.test.ts` (fail-open con warn, respuesta constante, 4º en la hora → 429, 6º del día → 429 y el 5º pasa, 21º por /64 → 429 sin gastar los contadores del email, Redis caído), `password-reset-confirm-route.test.ts` (marca `emailVerified` en la misma transacción, conserva la fecha previa, expirado/usado/inexistente → 400 sin tocar al usuario), `rate-limit-ip.test.ts` (`getRequestIp`).
- **Revertir:** en `route.ts` del signup volver `checkSignupRateLimit` a un booleano con una sola pipeline; en `complete/route.ts` sacar el `user` del select y el bloque 422; en el wizard sacar `verifyEmailHref`. Segunda pasada: en `verify-email/send` volver `checkSendRateLimit` al booleano con la clave por email; en el signup volver el honeypot a `200 {ok:true}` y sacar `mismaIpQueElReferidor`; en `confirm/route.ts` dejar `data: { passwordHash }` y en `findUserByResetToken` sacar el `user` del select. Los tests de D26 se caen solos.

## D27 · GraphQL para la app pública de Shopify: adaptador a la forma REST, modo `auto` por tenant, REST intacto para los clientes actuales
- **Hecho verificado (2026-09-01):** "Starting April 1, 2025, all new public apps must be built exclusively with the GraphQL Admin API" (shopify.dev/docs/api/admin-rest). La app pública nueva de AutoEnvía (client_id `a4231e6c…`, creada hoy) cae ahí: el primer install real en `autoenvia-qa.myshopify.com` pasó OAuth y murió en el callback con `shop_info_failed` porque `fetchShopInfo` hacía `GET shop.json` (REST). TODO el código saliente del worker era REST 2024-01 (`shopify/client.ts`, `orders.ts`, `fulfillment.ts` y 4 GET directos en los jobs). Los clientes con token de custom app (creadas antes) pueden seguir usando REST y **tienen que seguir exactamente igual**.
- **Decidido — dos caminos y una fachada, cero cambios en el camino REST:**
  - `apps/worker/src/shopify/graphql-client.ts`: `POST /admin/api/2026-07/graphql.json` con `X-Shopify-Access-Token`, timeout 20 s. Lee SIEMPRE `errors[]` aunque el HTTP sea 200: `THROTTLED` → espera `ceil((requestedQueryCost − currentlyAvailable) / restoreRate)` s (mín. 1) y reintenta hasta 3; red/5xx/429 → 500 ms, 1 s, 2 s; `MAX_COST_EXCEEDED` y `ACCESS_DENIED` salen tipados (`ShopifyGraphqlError.code`); errores parciales por `path` con `data` presente (datos protegidos de cliente sin aprobar) NO abortan, se avisan una vez por tienda y el campo queda en null. 401/402/403/423 no se reintentan. El token no aparece en ningún mensaje ni log. Los códigos `THROTTLED`, `MAX_COST_EXCEEDED`, `ACCESS_DENIED`, `SHOP_INACTIVE`, `INTERNAL_SERVER_ERROR` y la forma de `extensions.cost` están verificados en shopify.dev/docs/api/admin-graphql.
  - `graphql-adapter.ts` (puro) + `orders-graphql.ts` + `fulfillment-graphql.ts`: **mismas funciones públicas y firmas** que `orders.ts`/`fulfillment.ts`, devolviendo el mismo `ShopifyOrder` de `types.ts` que consume `dac/shipment.ts` (intocable): `id = Number(legacyResourceId)` (mismo número que el id REST guardado en `Label.shopifyOrderId`), `tags` CSV, `note_attributes` con `key→name`, `fulfillment_status` desde `displayFulfillmentStatus` (FULFILLED→`fulfilled`, PARTIALLY_FULFILLED→`partial`, RESTOCKED→`restocked`, resto null), `total_price` desde `totalPriceSet` (antes de devoluciones, como REST), direcciones con `?? ''`, `customer: null` (la app no tiene `read_customers`: `resolveOrderPhone` pierde las prioridades 3 y 5 y las reglas por `customer.tags` no matchean en tenants App Store). Listado con `orders(query: "financial_status:paid status:open", sortKey: CREATED_AT, reverse)` — **sin `fulfillment_status:`** (revisión 2026-09-01): en 2026-07 ese filtro tiene valores separados `unshipped|shipped|fulfilled|partial|scheduled|on_hold|unfulfilled|request_declined` (doc queries/orders) y no está documentado que `unfulfilled` incluya los parciales ni los on_hold/scheduled/in_progress que REST `fulfillment_status=unfulfilled` sí devuelve (null o partial; con `fulfillMode: always` el worker fulfilla FO on_hold/scheduled/in_progress). El equivalente exacto de REST se aplica del lado nuestro (`isRestUnfulfilled`: `fulfillment_status` null o `partial`; FULFILLED y RESTOCKED afuera), el tope de 250 cuenta los que pasan el filtro, y el escaneo corta en 1000 crudos con warn (tienda con auto-archivado apagado no cuelga el ciclo). Se pagina por cursor empezando en `first: 25 / lineItemsFirst: 50` y partiendo a la mitad si Shopify responde `MAX_COST_EXCEEDED` (la doc no publica la fórmula de costo de conexiones; el cliente es adaptativo). **Datos protegidos de cliente**: tras cada página se mira `lastErrors`; si hay un `ACCESS_DENIED*` por path sobre `shippingAddress`, `email` o `phone` del pedido, se lanza `ShopifyProtectedDataError` (en `shopify/errors.ts`, re-exportado por la fachada sin cargar GraphQL) ANTES de devolver pedidos: el job aborta ese tenant con un solo error accionable en el runlog en vez de escribir "LabelFlow ERROR: No shipping address" en cada pedido cada 15 minutos. Los errores por path en campos no portantes siguen tolerándose. Tag con `tagsAdd` (atómico, sin GET previo; NUNCA `orderUpdate(tags)`, que pisa). Nota con `orderUpdate(input: { id, note })` sólo con esas dos claves. Fulfillment con `order { displayFulfillmentStatus fulfillmentOrders { id status } }` + `fulfillmentCreate(fulfillment: { lineItemsByFulfillmentOrder, trackingInfo { number url company }, notifyCustomer: true })` — sin `fulfillmentOrderLineItems`: "If you don't specify line items, then the mutation fulfills all items in the fulfillment order" (doc verificada), igual que el POST REST. `ACCESS_DENIED` → `ShopifyMissingScopesError` (misma instrucción para el operador); `userErrors` → `Error` con campo y mensaje.
  - `mode.ts`: `SHOPIFY_API_MODE=rest|graphql|auto` (default `auto`, para forzar en emergencias). En `auto`: slug **exactamente igual** a `tenantSlugForShop(storeUrl)` (réplica en el worker de la regla de `apps/web/lib/shopify-provision.ts`: ≤40 → `shop-<handle>`, >40 → `shop-<31>-<sha256[0:8]>`; test con vectores fijos) → GraphQL, sin fallback a REST (la app pública no puede usarlo). **No es un prefijo** (revisión 2026-09-01): los slugs manuales se derivan del local-part del email o del nombre de la tienda + sufijo (`signup/route.ts`, `lib/auth.ts`, `tenants/route.ts`), así que `shop@marca.com` → `shop-m1abcd` y una tienda "Shop Marca" → `shop-marca-xxxxxx` tienen token de custom app y se quedan en REST. Cualquier otro slug → REST. Conmutación REST→GraphQL sólo con **match positivo**: 403 cuyo cuerpo mencione la `REST Admin API` (patrón INFERIDO: la doc no publica el texto y no está verificado — PENDIENTES tiene el curl), nunca si contiene `required permission` (scopes → `ShopifyMissingScopesError`) ni `merchant approval` (`read_orders` / `read_all_orders` para pedidos de más de 60 días, con token de custom app que sí usa REST — community.shopify.com hilo 24159), y **nunca para un tenant al que REST ya le respondió 2xx en este proceso** (`markRestWorking`): ese tenant no puede ser "app sin REST", así que cualquier 403 posterior sube tal cual. Si el texto real del 403 no matchea, el cliente con slug viejo que pasa por **Reconectar** con token de la app pública ve un 403 claro en el runlog (fallo visible), no una degradación silenciosa; se arregla ajustando `REST_403_FORBIDDEN_PATTERNS`. Sin columna en la base (sin migración): la memoria se pierde al reiniciar, y está bien.
  - `shopify/index.ts` (fachada): `createShopifyClient(storeUrl, token, { tenantId, slug })` y `getUnfulfilledOrders / getRecentOrders / addOrderTag / addOrderNote / markOrderProcessed / fulfillOrderWithTracking` con las mismas firmas. Para un tenant en modo rest llama a `orders.ts`/`fulfillment.ts` con el axios de `client.ts` de siempre (mismos endpoints 2024-01, mismos parámetros, mismos logs; los tres archivos no cambiaron ni una línea). Los módulos GraphQL se cargan con `import()` sólo cuando hace falta: con `SHOPIFY_API_MODE=rest` el proceso nunca los carga (test `shopify-facade.test.ts`). `client.get('/orders/{id}.json')` se mantiene para los 4 GET directos de los jobs (`data.order?.note`); en GraphQL es un shim con esa misma forma y cualquier otro path se rechaza claro.
  - Jobs (`process-orders`, `process-orders-bulk`, `agent-bulk-upload`, `test-dac`), `self-delivery/process.ts` y `dac/finalize-recovered-guias.ts`: sólo cambian los imports (a la fachada), el tipo del cliente (`ShopifyClient`) y la línea de creación, que ahora pasa `{ tenantId: tenant.id, slug: tenant.slug }`. Ninguna otra línea de lógica.
  - `apps/web`: `fetchShopInfo` → `query { shop { name email myshopifyDomain } }` (campos verificados en objects/Shop 2026-07; `email` no está deprecado) y `registerShopifyWebhooks` → `webhookSubscriptions(first: 50, topics)` para la idempotencia + `webhookSubscriptionCreate(topic, webhookSubscription: { uri, format: JSON })` (`callbackUrl` deprecado; `uri` vigente), topics `ORDERS_PAID`/`APP_UNINSTALLED` sólo hacia Shopify — el receptor sigue leyendo `X-Shopify-Topic` como `orders/paid`. Sin modo: las dos corren únicamente con tokens OAuth de la app pública. Mismas firmas y mismo contrato `registered/alreadyPresent/failed` (`failed[].status` = 200 con `userErrors` en `body` cuando Shopify rechaza con HTTP 200).
- **Lo que NO está verificado y hay que chequear en `autoenvia-qa` antes de apoyarse (ver PENDIENTES.md):** (1) qué HTTP y qué cuerpo devuelve REST a la app pública (la doc no lo dice; el selector conmuta sólo con 403 + cuerpo que mencione `REST Admin API` — si no matchea, no conmuta nunca y el 403 sube claro; los tenants del App Store no dependen de eso); (2) que el set de ids de `financial_status:paid status:open` filtrado del lado nuestro coincida con el de REST `fulfillment_status=unfulfilled` (un pedido on_hold + uno parcial, diff de ids; la semántica REST "null o partial" es de memoria, la página REST no rinde la tabla de parámetros); (3) el costo real de la página inicial (`first: 25 / lineItemsFirst: 50`) y cuántos crudos escanea una tienda con auto-archivado apagado; (4) el texto exacto del `ACCESS_DENIED` y del `userError` de webhook duplicado (el diseño no depende de ellos); (5) datos protegidos de cliente: sin la aprobación en el Partner Dashboard el Admin API devuelve nombre/dirección/email/teléfono en null → el worker ahora aborta el tenant con `ShopifyProtectedDataError` (un solo error, sin notas por pedido), pero el trámite sigue siendo de Adrian y en tiendas de desarrollo no se reproduce. Nada de esto es de código.
- **Revertir:** `SHOPIFY_API_MODE=rest` en Render deja todo como antes sin deploy (los módulos GraphQL ni se cargan). Para sacar el código: borrar `shopify/{graphql-client,graphql-adapter,orders-graphql,fulfillment-graphql,mode,errors,index}.ts` y volver los imports de los 6 archivos a `../shopify/client|orders|fulfillment`. En web, `git show c79e218^:apps/web/lib/shopify-provision.ts` y `…/shopify-register-webhooks.ts` son las versiones REST.

## D28 · Fuera del alcance de D27: lo que sigue en REST y cómo degrada para un tenant del App Store
- **Sigue en REST a propósito** (tokens manuales de custom app, o módulos que no están en el flujo de despacho): `settings PUT` y `onboarding/test-shopify` (validan un token pegado a mano contra `shop.json` — un token de la app pública nunca pasa por ahí), `shopify-scopes` (`access_scopes.json`), `products/scan` (catálogo para product-filter), `fulfill-retry` (reintento manual desde el dashboard: duplica la lógica del worker en web), `clean-notes`, `lib/shopify-reconcile.ts`, `lib/shopify-pending.ts` (contador del dashboard), `lib/recover-setup.ts` + `app/api/recover/sync` (`checkouts/*`, módulo Recover), `lib/wms-items-backfill.ts`.
- **Cómo degrada hoy si un tenant `shop-*` las toca (verificado leyendo cada ruta, no se cambió código):** ninguna hace crash. `shopify-scopes` → 502 "Error consultando scopes de Shopify" (log con status y cuerpo recortado); `products/scan` → 401/403 se trata como "sin scope", devuelve `false` y el filtro sigue sin catálogo; `fulfill-retry` → el `!foRes.ok` responde el error de Shopify (sin `required permission` no es el mensaje de scopes, es el genérico); `clean-notes` → error del `catch` en la respuesta; `shopify-reconcile` y `wms-items-backfill` → lanzan y sus llamadores ya envuelven en try/catch ("nunca rompe el export"); `shopify-pending` → `catch` y contador vacío; `recover-setup`/`recover/sync` → `failed` / error de respuesta. El síntoma común para un tenant App Store es "esta función no anda" con un log 4xx de REST, no un 500.
- **Qué falta para cerrarlas (cada una es una rama chica sobre `lib/shopify-graphql.ts`, que ya existe):** `access_scopes.json` → `query { currentAppInstallation { accessScopes { handle } } }`; `products.json` → `products(first, after)`; `fulfill-retry` → reutilizar la lógica de `fulfillment-graphql.ts` (o mejor: encolar el job del worker en vez de duplicar); `clean-notes` → `orders(query: "name:#N")` + `orderUpdate(note)`; `shopify-pending`/`reconcile`/`wms-items-backfill` → `orders(query)` con `ordersCount` para el contador; Recover → `webhookSubscriptionCreate` con `CHECKOUTS_CREATE/UPDATE` y `abandonedCheckouts` (nombres a verificar en 2026-07 antes de usar). No se hizo en D27 para no ensanchar la rama: el despacho (worker) y el alta (callback) eran lo que estaba roto.

## D29 · Tokens offline EXPIRABLES de la app pública: el par entero cifrado en `Tenant.shopifyToken`, renovación bajo demanda, sin migración
- **Hecho verificado (2026-09-01, cuerpo del error en logs de producción):** al pedir `shop {}` por GraphQL con el token del install de la app pública (client_id `a4231e6c…`, creada hoy), Shopify responde HTTP 403 `[API] Non-expiring access tokens are no longer accepted for the Admin API. Start using expiring offline tokens`. El canje de `/callback` pedía el token sin `expiring`, así que TODO install del App Store terminaba con un token que Shopify rechaza. Los tokens legacy de custom app (los clientes actuales) no vencen y siguen exactamente igual.
- **Protocolo (shopify.dev, authorization-code-grant y offline-access-tokens):** canje `POST https://{shop}/admin/oauth/access_token` JSON `{ client_id, client_secret, code, expiring: '1' }` → `{ access_token, scope, expires_in: 3600, refresh_token: 'shprt_…', refresh_token_expires_in: 7776000 }`. Refresh: mismo endpoint, JSON `{ client_id, client_secret, grant_type: 'refresh_token', refresh_token }` → `{ access_token, expires_in, refresh_token, refresh_token_expires_in, scope }`. **Cada refresh rota**: el refresh viejo deja de servir en el acto. Un fallo de red DESPUÉS de que Shopify rotó (p.ej. un 520) deja huérfano el par guardado. Access 1 h, refresh 90 días.
- **Decidido — sobre cifrado, sin schema:** el texto plano de `Tenant.shopifyToken` (AES-256-GCM de siempre) pasa a ser un JSON v1 `{"v":1,"access":"shpat_…","exp":<epoch ms>,"refresh":"shprt_…","refreshExp":<epoch ms>}`. Un texto plano que no empieza con `{` es un token legacy y se usa tal cual. Módulo `apps/web/lib/shopify-token.ts` y su copia `apps/worker/src/shopify/token.ts` (mismo contenido, cada app con sus imports): `parseShopifyCredential` / `serializeShopifyCredential` / `isExpiringSoon` (margen 5 min) / `refreshShopifyCredential` (timeout 15 s; `ShopifyRefreshInvalidGrant` para 400/401 con `invalid_grant` o "token inválido", `ShopifyRefreshTransient` para red/timeout/429/5xx) / `getValidShopifyAccessToken` (y `resolveShopifyAccessToken`, que además devuelve el motivo para el runlog). `shopify-access.ts` / `shopify/access.ts` son los atajos con el Prisma y el `SHOPIFY_API_KEY`/`SECRET` de cada app.
  - **Renovación bajo demanda** (Vercel Hobby no tiene cron sub-diario): quien necesita el token llama a `getValidShopifyAccessToken`. Legacy → el mismo string que `decryptIfPresent`. Envelope fresco → access sin tocar Shopify. Vence en < 5 min y hay secret → refresh y **persistir ANTES de devolver**: `UPDATE "Tenant" SET shopifyToken = <nuevo cifrado> WHERE id = ? AND shopifyToken = <cifrado que leí>` (dos intentos). 0 filas = otro proceso rotó, un reinstall escribió otro par o `uninstalled` lo puso en null: se relee y se usa lo que haya, nunca se pisa. `invalid_grant` → releer; si la base cambió, ese par es el bueno; si no, el guardado quedó huérfano: **NO se borra**, se loguea `la tienda tiene que reinstalar la app` y se devuelve null. Fallo transitorio → si el access todavía sirve se usa; si ya venció, null con motivo reintentable. Sin secret (el worker hoy, ver PENDIENTES) → warn UNA vez por tenant por proceso y el access guardado. Dentro de un proceso, dos llamadas concurrentes para el mismo tenant comparten un solo refresh. Ninguna función loguea tokens.
  - **Escritura:** `/callback` (ramas A y B) canjea con `expiring: '1'`; si la respuesta trae `refresh_token` guarda `encrypt(serialize(envelope))`, si no (app vieja sin tokens expirables) guarda el access pelado como legacy. `missing_scopes` se sigue evaluando con `json.scope`. `provisionFromShopify` recibe el texto plano a guardar (no lo usa contra Shopify). La cookie pendiente (`shopify-pending-install`, sellada como siempre con el prefijo de dominio) transporta el envelope entero y `/claim` lo persiste tal cual, hablando con Shopify sólo con el `access`. `uninstalled` sigue poniendo `shopifyToken` en null. `settings PUT` y `onboarding/test-shopify` (token pegado a mano) no cambian: escriben legacy.
  - **Lectura:** en web, todo `decryptIfPresent(tenant.shopifyToken)` / `decrypt(...)` de `shopify-scopes`, `products/scan`, `fulfill-retry`, `clean-notes`, `recover/sync`, `shopify-reconcile`, `shopify-pending`, `recover-setup` (las dos funciones) y `wms-items-backfill` (la credencial ahora lleva el `id`) pasa por `shopifyAccessForTenant`. `/api/v1/jobs` y `/api/v1/control/run` llaman a `warmShopifyToken` **antes** de encolar: un "Ejecutar" manual arranca con par fresco aunque el worker no tenga el secret. En el worker, `process-orders`, `process-orders-bulk`, `agent-bulk-upload` y `test-dac` usan `resolveShopifyAccessForTenant`; si devuelve null, el job falla con el mensaje accionable (`reinstall` / `refresh-failed`) en vez del genérico "Missing Shopify config". `scheduler.ts` (sólo filtra `shopifyToken not null`) y `shopify/mode.ts` no cambian.
  - **Revisión (mismo día), dos hallazgos altos cerrados:**
    - *Carrera entre procesos.* Dos procesos sin memoria compartida (dos invocaciones de Vercel — el dashboard dispara `control/pending` y `shopify-scopes` en paralelo —, o web + worker) leen el mismo envelope por vencer y piden el refresh con el mismo `shprt`; Shopify rota para uno y el otro recibe `invalid_grant` **antes** de que el ganador haga su UPDATE. Con una relectura inmediata el perdedor declaraba `reinstall` (mensaje al operador, job fallido) aunque la base tuviera un par válido 100 ms después. Ahora `invalid_grant` → relectura; si la base no cambió, espera `SHOPIFY_TOKEN_RACE_GRACE_MS` (1,5 s) y relee **una** vez más; si sigue igual y el access viejo todavía no venció, se usa ese con un warn (la próxima llamada reintenta); `reinstall` queda para access vencido + dos relecturas estables. Se descartó el `pg_advisory_xact_lock` dentro de `$transaction`: implica sostener una conexión de Postgres (pgbouncer, Vercel) durante un HTTP a Shopify de hasta 15 s.
    - *Token estático dentro de un job.* El margen de 5 min sólo cubría el arranque; `process-orders` construía `createShopifyClient` una vez y un job de horas (Playwright por pedido, `maxOrdersPerRun` 0) cruzaba el vencimiento con la guía DAC ya creada y pagada: fulfillment + tag fallaban como "non-fatal". Ahora (a) `skewMs` es parámetro de `resolveShopifyAccessToken`; `SHOPIFY_TOKEN_JOB_SKEW_MS` (55 min) lo usan `warmShopifyToken` en la web y `resolveShopifyAccessForJob` al inicio de los 4 jobs: cada corrida arranca con ~1 h entera (una rotación por corrida). (b) `createShopifyClient` acepta `string | ShopifyTokenProvider`: legacy → string fijo (camino REST byte a byte, sin lecturas extra); expirable → `shopifyTokenSourceForTenant`, un proveedor que en cada request relee la fila del tenant (ve rotaciones de la web/otro proceso) y resuelve; el cliente GraphQL, ante un 401, le pide `forceRefresh` (= `skewMs: Infinity`) y reintenta **una** vez sólo si el token cambió; REST resuelve el header por request y no reintenta (la app pública no tiene REST, los legacy no vencen). Sin secret el aviso "no se puede renovar" sólo sale cuando vence en < 5 min, no cada vez que un job pide el margen largo. La resolución ahora trae `legacy` para que el job sepa qué pasarle al cliente.
- **Lo que queda afuera y por qué:** (1) el worker sin `SHOPIFY_API_KEY`/`SECRET` en Render no renueva: el proveedor por request relee la base y **ve** lo que rotó la web, pero no rota él mismo; un tenant del App Store que no pasa por la web en 1 h llega al cron con el access vencido y el job falla con `refresh-failed`/401 (el reintento por 401 no repite si el proveedor devuelve el mismo token) hasta que alguien toque la web o Adrian cargue las vars (PENDIENTES). (2) Los tenants del App Store instalados ANTES de este cambio tienen un token no expirable que Shopify ya rechaza: reinstalar (Settings → Reconectar). (3) Si Shopify rota y el UPDATE falla dos veces, esta corrida sirve y la siguiente cae en `invalid_grant`; mientras el access viejo viva se usa ese, y con el access vencido → reinstall; queda dicho en el log. (4) El texto exacto del cuerpo de `invalid_grant` no está verificado contra una tienda real: el patrón acepta `invalid_grant` y variantes de "invalid token". (5) **Supuesto no verificado** (ni en shopify.dev ni contra una tienda real): que el access anterior siga sirviendo hasta su `exp` después de que otro proceso rotó el refresh. Si Shopify lo revocara al rotar, el perdedor de la carrera recibiría un 401 en su próxima request y el proveedor por request (worker) o la próxima ruta (web) lo resolverían con el par nuevo de la base.
- **Revertir:** volver `/callback` a canjear sin `expiring` y los consumidores a `decryptIfPresent`; los envelopes ya guardados se leen igual (legacy passthrough no aplica: un `{` en el texto plano sería un token inválido) → los tenants del App Store tendrían que reconectar.


## D30 · La app del Shopify App Store es GRATUITA; el cobro vive en autoenvia.com (Coins) con MercadoPago y Whop
- **Decidido (Adrian, 2026-09-02):** la app se lista gratis. Los envíos se compran en autoenvia.com: MercadoPago (flujo de packs existente, `credit-packs/checkout` + webhook `x-signature`) y Whop como segundo riel (link de checkout por pack en `WHOP_CHECKOUT_URLS` + webhook `POST /api/webhooks/whop`). Se conserva D8: Whop nunca reemplaza a MercadoPago.
- **Riesgo asumido:** Shopify exige su Billing API para cobrar por apps distribuidas en el App Store; cobrar afuera puede chocar con la revisión o los términos del Partner Program. Adrian lo asume. Si la revisión lo rechaza, el camino es Billing API (0 % hasta USD 1M según PENDIENTES) en otra rama.
- **Spec:** `docs/SELFSERVE-SPEC.md` §7.

## D31 · 5 envíos gratis por CUENTA nueva, explícitos en cada alta; el default del schema (10) no se toca
- **Hallazgo:** hoy el trial sale del `@default(10)` de `Tenant.shipmentCredits` en cuatro creates que no pasan el valor (signup, Google OAuth ×2, App Store `created`), y `WELCOME_BONUS_SHIPMENTS`/`REFEREE_BONUS_CREDITS` están duplicados. Reemplaza a D7 en lo que decía "5 por tienda atado a shopDomain".
- **Decidido:** constante compartida `TRIAL_SHIPMENTS = 5` en `apps/web/lib/trial.ts`, pasada explícita en el create del signup público y en `provisionFromShopify` (`created`) y en los dos creates de Google OAuth. Se acredita una sola vez al tenant holder (el primero del user). Tiendas adicionales (`POST /api/v1/tenants`) y reclamadas (`/api/shopify/claim`) siguen naciendo con 0. Un dominio de Shopify ya vinculado a otro tenant devuelve `conflict` y no crea nada. **No se cambia el schema**: el default 10 queda y por eso el explícito es obligatorio en cada alta (test que lo fija). El bono de referido (`referralBonusCredits`, 10) no cambia.
- **Spec:** `docs/SELFSERVE-SPEC.md` §3.

## D32 · Menú por rol: el usuario ve Dashboard, Etiquetas y Configuración; el admin (ADMIN_EMAILS) ve todo, Control incluido
- **Mecanismo:** el que ya existe — `ADMIN_EMAILS` (lista separada por comas, fallback `ADMIN_EMAIL`, no es secreto) en `apps/web/lib/admin.ts`; sin lista nadie es admin. Se agrega `isAdminEmail()` puro para resolverlo en `(dashboard)/layout.tsx` sin query extra y pasarlo al Sidebar y a un `RoleProvider`.
- **Decidido:** usuario normal = `/dashboard` (envíos hoy/mes, tasa de éxito, saldo, Procesar ahora), `/labels` (con Imprimir todas) y `/settings` (Tiendas, DAC, Reglas de envío, Parámetros, Comprar envíos). `/control`, `/orders`, `/reports` devuelven 404 server-side a no-admin (mismo patrón que `/admin`). `/settings/referrals` sigue accesible (el TopBar linkea ahí cuando hay bonus). Control no se amplía: lista los tenants del user logueado (métricas globales = `/admin`, que pasa al menú de admin).
- **Spec:** `docs/SELFSERVE-SPEC.md` §4 y §8.
- **Revisión 2026-09-02 (Control del admin):** Adrian pidió que cuando un cliente completa el onboarding su tienda le aparezca en Control desde su cuenta admin. Retira el "Control no se amplía" de arriba: para un admin (`ADMIN_EMAILS`, resuelto contra la fila del User igual que `getAdminSession`) `GET /api/v1/control/overview` lista sus tenants **más todos los activos de todos los usuarios**, cada uno con `owner: { email, own }` y `adminView: true`; la página agrupa por dueño (propias primero). Las ocho rutas de `/api/v1/control/*` (overview, pending, run, retry, labels, recent-labels, shipments, labels/[id]/pdf) arman el alcance con el único helper `lib/control-scope.ts` (`controlTenantWhere`: usuario → `{ userId }`; admin → `{ OR: [{ userId }, { isActive: true }] }`), así lo que no se lista tampoco se opera: una tienda ajena **inactiva** le da al admin el mismo 403. El gate de plan/saldo de Ejecutar y Reintentar se evalúa sobre el holder del **dueño** de la tienda, y el wallet del overview es el del que mira. Para un usuario normal la respuesta y la autorización son idénticas a las de antes (test `control-admin-routes.test.ts`). **Eliminar** sigue siendo sólo del dueño (`DELETE /api/v1/tenants/[id]` no cambia: borrar la tienda de un cliente es irreversible y no fue pedido).

## D33 · Onboarding obligatorio de 6 pasos, con el estado derivado de la base (sin columnas nuevas)
- **Decidido:** (1) Bienvenida con checklist y tiempos; (2) Conectar tienda: "Instalar la app de Shopify" (`/api/shopify/install?shop=…&next=/onboarding`, con token manual como fallback plegado) o "Dashboard con Excel" (fuente `dashboardUrl` + `dashboardToken`, probada contra `GET /api/v1/orders?limit=1`); (3) Cuenta DAC (`onboarding/test-dac`, que no verifica en vivo: el copy lo dice); (4) Parámetros con explicación, para quién y ejemplo, sólo los que el producto soporta; (5) Modo: Inmediato (`*/15 * * * *` + webhook cuando existe) o Cada hora (`0 * * * *`) sobre `cronSchedule`; (6) Listo con los 5 envíos y "Activar y procesar ahora" (`onboarding/complete` + `POST /api/v1/jobs`).
- **Derivación:** tienda conectada = Shopify (url+token) **o** Dashboard (enabled+url+token); DAC = usuario+contraseña; completo = `onboardingComplete`; modo = `cronSchedule`. Helper único `lib/onboarding-state.ts` usado por el gate del dashboard, por `complete` y por `GET /api/v1/onboarding/state`. El gate deja de exigir Shopify (aceptaba sólo Shopify+DAC) y deja de backfillear `onboardingComplete` a tenants inactivos.
- **Contrareembolso:** existe en main (`Tenant.codEnabled`, `Label.codAmount`, `planDeCod`), pero sin UI ni entrada en `settings`. Se expone como toggle sobre la columna existente; precondición de deploy: verificar la columna en prod (SQL de `scripts/sql/cod-contrarreembolso.sql`). Lo que NO existe (filtro por SKU exacto, "siempre paga la tienda", pago automático con tarjeta, WhatsApp al cliente, transportista alternativo) no se inventa: `docs/SELFSERVE-SPEC.md` §6.4.
- **Spec:** `docs/SELFSERVE-SPEC.md` §5 y §6.
- **Revisión 2026-09-02 (tres hallazgos cerrados):** (1) Un tenant completo que pierde la tienda o DAC (desinstala la app desde Shopify → `uninstalled` deja `shopifyToken` null; borra un token) ya no rebota entre `/dashboard` y `/onboarding`: `/onboarding` sólo redirige al dashboard si completó **y** sigue conectado y no vino con `?step` ni con `?shopify=` (retorno del OAuth), y `deriveOnboarding` devuelve 2/3 en vez de 6 cuando falta algo (`shouldRedirectToDashboard` en `lib/onboarding-state.ts`, tests en `onboarding-page.test.ts`). (2) El webhook `orders/paid` respeta **Cada hora**: con `cronSchedule = 0 * * * *` guarda el receipt y responde 200 sin encolar; el cron en punto lo levanta. Inmediato y los crons a medida del admin siguen encolando (`shopify-webhook-route.test.ts`). (3) Con **Dashboard con Excel** el paso 4 y Configuración > Parámetros no ofrecen los ocho parámetros que `PROCESS_DASHBOARD_ORDERS` ignora (quién paga, envío gratis, pedidos seguidos, productos, SKU, contrareembolso, email, orden): un solo aviso "todas las guías salen pagas por el cliente al recibir" y Continuar. Soporte en el worker: PENDIENTES.
- **Revisión 2026-09-02, segunda pasada (dos hallazgos altos):** (1) `Tenant.codEnabled` y `Label.codAmount` **están en prod** (verificado con `information_schema` sobre `DIRECT_URL`; el encabezado del SQL decía "NO APLICADO" y se corrigió). Lo que no se pudo verificar desde acá es el commit que corre el worker en Render (autoDeploy OFF, el MCP exige que Adrian elija el workspace): sin `df13204` el toggle "todas las guías salen como contrareembolso" mentiría. Decidido: `COD_FEATURE_ENABLED` (`lib/cod-feature.ts`, no es secreto, default apagada, fail-closed): `GET /api/v1/settings` devuelve `codAvailable`, `PUT` rechaza `codEnabled: true` con 422 (apagar siempre se puede), y el bloque del paso 4 / Configuración se muestra como "Próximamente" sin toggle. Adrian la prende en Vercel cuando confirme el deploy del worker; no hace falta otro deploy de la web. (2) Con Dashboard con Excel, "Procesar 1 pedido" encolaba `PROCESS_DASHBOARD_ORDERS` con un RunLog `maxOrdersOverride=1` que ese job ignora (`DASHBOARD_FETCH_LIMIT=100`, recorta sólo por saldo): habría despachado hasta 100 guías reales. Decidido: `POST /api/v1/jobs` responde 422 si `kind === 'dashboard'` y hay límite (`maxOrders` o `testMode`), sin crear el job; el Dashboard esconde los chips 1/3/5/10/20 para esos tenants y el botón dice "Ejecutar pedidos confirmados" con body `{}`. `control/run` no cambia: siempre encola el job de Shopify, que en un tenant de Excel falla sin despachar. Que el job de Dashboard lea el override es cambio del worker (PENDIENTES).

## D34 · Comprar envíos: selector "¿cuántos envíos hacés por mes?" sobre los tramos vigentes, MercadoPago y Whop con la misma acreditación
- **Decidido:** precio por envío según los tramos 20/17/15/12/10/7 UYU (`credit-packs.ts` = `worker/billing/tiers.ts`, cortes 0/50/100/250/500/1000), pack recomendado = el más chico que cubre el volumen, total y ahorro frente a comprar de a 10. Botón MercadoPago = flujo de packs existente. Botón Whop = `GET /api/credit-packs/whop-checkout?pack=` que crea el `CreditPurchase` PENDING (`mpExternalRef = 'whop|<id>'`) y redirige a la URL de `WHOP_CHECKOUT_URLS` (JSON `{packId: url}`; sin URL el botón no existe).
- **Webhook Whop:** Standard Webhooks (`webhook-id`, `webhook-timestamp`, `webhook-signature`; HMAC-SHA256 base64 de `id.timestamp.body`; secret `WHOP_WEBHOOK_SECRET`, con o sin prefijo `whsec_`; tolerancia 5 min; 503 si falta el secret), dedupe por entrega en `WebhookReceipt(source='whop')`, idempotencia por pago con `mpPaymentId = 'whop:<pay_id>'` (`@unique`), holder resuelto por `metadata.purchaseId` → `metadata.userId` → email del pago, y **no acredita** si la compra es ambigua (queda logueado para acreditar a mano). Nunca loguea el cuerpo.
- **Producto (revisión 2026-09-02):** los links de checkout de Whop son públicos y no llevan metadata, así que la compra PENDING no prueba QUÉ se pagó. Env `WHOP_PLAN_IDS` (JSON `{packId: plan_id}` o `{packId: {planId, minUsd}}`, no es secreto): el webhook acredita sólo si el plan/producto del evento es el del `packId` de la compra y, con `minUsd`, si el monto es en USD y ≥ el piso. Sin la var, sin plan en el payload o con plan distinto → `flagged` y 0 envíos. `WHOP_CHECKOUT_URLS` no se prende sin `WHOP_PLAN_IDS`.
- **Misma función:** la acreditación de MP (`webhooks/mercadopago/route.ts:373-380` + kickback) se extrae a `lib/credit-accrual.ts` (`settlePaidPurchase`, `failPendingPurchase`, `refundPaidPurchase`, `accrueReferralKickback`) sin cambiar semántica, y MP y Whop la llaman.
- **Spec:** `docs/SELFSERVE-SPEC.md` §7.
