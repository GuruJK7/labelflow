# Pendientes — datos que faltan y dónde conseguirlos

Cada `[[COMPLETAR]]` del prompt maestro que quedó vacío, con lo que **sí** se
pudo verificar y lo que sigue faltando. Nada de esto se inventó.

| Ítem | Estado | Dónde se consigue |
|---|---|---|
| `SHOPIFY_API_SECRET` en Vercel | **FALTA** — sin esto OAuth y webhooks están apagados (fail-closed) | Dev Dashboard → app `417965080577` → Configuración → Credenciales → ojo. `npx vercel env add SHOPIFY_API_SECRET production --scope gurujk7s-projects`. Sólo Adrian. |
| App URL de Shopify → `/api/shopify/entry` | **FALTA** — hacerlo **después** del deploy de PR #3 | Dev Dashboard → Versiones → Crear versión → URL de la app |
| Aceptación de términos en el primer login (tenants aprovisionados desde el App Store) | **FALTA** — `provisionFromShopify` y `/api/shopify/claim` dejan `tosAcceptedAt = null` a propósito (D14): el comerciante autorizó la app en Shopify, no aceptó nuestros términos. Hoy ningún gate lo exige (verificado por grep), así que entra igual. Hace falta un interstitial en el primer login (o en la página de set-password) que lo pida y setee `tosAcceptedAt` + `signupIp`. | Código: gate en `(dashboard)/layout` o en `/reset-password/[token]`; decisión de copy legal de Adrian |
| Revocar tokens de Shopify emitidos y descartados | **PARCIAL** — la rama dashboard ya no canjea el `code` si va a fallar (D13); la rama App Store canjea antes de saber de quién es la tienda, y en `conflict`/`shop_info_failed` el token queda emitido sin guardar. Shopify no expone revocación por API sin el token del comerciante; queda como está y se documenta. | — |
| Migración de clientes con token manual (custom app) al OAuth público | **PENDIENTE — a mano, por cliente** — `/api/shopify/entry` cortocircuita el OAuth si el tenant ya tiene `shopifyToken`, sin distinguir si ese token vino por OAuth o pegado a mano desde una custom app (D16: no hay flag de origen sin migración). Para esos clientes, apretar "Instalar" en el App Store NO registra la instalación pública en Shopify: cae en `/login?shopify=open`. Camino: el cliente entra a Settings → **Reconectar**, que sí pasa por OAuth y deja el token público. Cuando todos los tenants vivos estén reconectados, el cortocircuito puede quedarse tal cual. | Lista de tenants con token manual: `select slug from "Tenant" where "shopifyToken" is not null` cruzada con quién pasó por `/callback` (no hay columna: se sabe por el soporte). Adrian decide si se avisa por WhatsApp uno por uno. |
| Migración `20260901180000_tenant_shop_domain_lower` (SÓLO `UPDATE … lower()` sobre `Tenant.shopifyStoreUrl`, en `BEGIN/COMMIT`) | **ESCRITA, 🔴 SIN APLICAR** (D9/D18) — `apps/web/prisma/migrations/20260901180000_tenant_shop_domain_lower/migration.sql`. El repo no usa `prisma migrate` (la carpeta no existía): se corre a mano sobre `DIRECT_URL` en otro turno. **Antes** correr los dos SELECT del encabezado (duplicados por `lower()` y filas afectadas) y guardar el resultado. No requiere decidir nada. El UPDATE **sí cambia comportamiento** y hace falta aunque `apps/web` ya compare insensible a mayúsculas: el worker (`sharedTenantIds` en `process-orders.job.ts`, el filtro anti-duplicado de `94a6282`) compara el dominio EXACTO, así que hoy un tenant con `MiTienda…` y otro con `mitienda…` sobre la misma tienda no se ven entre sí y pueden despachar el mismo pedido dos veces; hasta que se aplique, ese hueco sigue abierto. | Sólo Adrian, con los dos SELECT en mano |
| Migración `20260901180100_tenant_shop_domain_unique` (índice único parcial sobre `Tenant.shopifyStoreUrl`) | **ESCRITA, 🔴🔴 DECISIÓN PENDIENTE — NO APLICAR** (D22). `main` soporta a propósito que dos tenants compartan tienda (`94a6282`, incidente Aura 2026-05-08; `sharedTenantIds` en el worker; `settings PUT` lo respeta desde D21). Aplicar el índice implica **retirar ese diseño**: una tienda = un tenant, los que hoy la comparten quedan con `shopifyStoreUrl NULL` salvo uno (saldo y etiquetas revisados a mano) y `sharedTenantIds` pasa a ser código muerto. Lo que se gana: cierra la carrera reclamo-vs-Conectar del dashboard. Orden estricto: decisión explícita → `…_lower` aplicada → SELECT de duplicados en 0 → recién ahí este archivo. | Decisión de Adrian, no del código |
| `SHOPIFY_API_KEY` y `SHOPIFY_API_SECRET` en el entorno del **worker** en Render (lo carga Adrian) | **FALTA** (D29) — sin ellos el worker no puede renovar el token expirable de un tenant del App Store: avisa una vez por tenant en el log (`SHOPIFY_API_KEY/SHOPIFY_API_SECRET no están en el entorno`, sólo cuando vence en < 5 min) y usa el access guardado, que dura 1 h. El proveedor por request de los jobs **ve** lo que rotó la web (relee la fila del tenant), pero no rota él mismo, y el reintento por 401 no repite si el token no cambió: **los crons del scheduler para un tenant del App Store que nadie tocó en la web en 1 h fallan con 401/`refresh-failed` hasta que las vars estén cargadas.** Mientras tanto renuevan la web (cualquier ruta que use el token) y el "Ejecutar" manual (`warmShopifyToken`, margen de 55 min, antes de encolar). Son los mismos valores que Vercel. | Render → worker → Environment. Sólo Adrian. |
| Tokens del App Store guardados **antes** de D29 (canje sin `expiring`) | **REINSTALAR** — Shopify ya los rechaza con 403 "Non-expiring access tokens are no longer accepted"; el código no puede convertirlos (no hay refresh). Cada tienda instalada desde el App Store antes de este deploy pasa por Settings → **Reconectar con Shopify** (o reinstala desde el App Store). Hoy sólo `autoenvia-qa`. | Lista: tenants con slug `shop-*` y `shopifyToken` cuyo texto plano no empieza con `{` (se sabe descifrando; no hay columna). |
| Contacto de emergencia (App Store) | **FALTA** — mail + teléfono | Partners → Revisión App Store |
| Acceso a datos protegidos de clientes | **FALTA** — apretar *Solicitar*, **no** marcar "no usa datos" (usa nombre, dirección, teléfono) | Partners → Revisión App Store |
| Capturas 1600×900 (3–6), video o imagen 1600×900, tienda demo | **FALTA** — necesita la app funcionando con una dev store | Se hacen después de conectar una tienda de desarrollo |
| ¿Shopify le paga a una entidad uruguaya? | **NO DETERMINADO** — decide si se puede cobrar con Shopify Billing (0 % hasta USD 1M) | Partners → Pagos → Configurar método de pago (ver qué rieles ofrece a UY) |
| Costo real por guía DAC | **NO DOCUMENTADO en ningún repo** — cada cliente usa su propia cuenta DAC; el flete no pasa por LabelFlow | Contrato/cuenta DAC del cliente |
| Costo mensual real Render/Supabase(x2)/Vercel | **NO DOCUMENTADO** | Facturas de cada servicio |
| Tipo de cambio UYU/USD | **PARCIAL (D35)** — en LabelFlow ya no está hardcodeado: es la env `USD_UYU_RATE` (default 40, el tipo del que salió la escalera). Sigue **sin verificar cuál es el real**, y el `44` sigue hardcodeado en los otros dos repos. 🔴 Subirlo sube en pesos lo que paga TODO cliente actual: a 44 suben los seis escalones viejos. | Cotización real → `npx vercel env add USD_UYU_RATE production --scope gurujk7s-projects`. Sólo Adrian. Si se quiere que cambie sin deploy, mover a una fila editable (otro turno). |
| Migraciones `0001–0003` de autoenvia-dash aplicadas en Supabase `zgptruicwqswtodgzfkp` | **NO VERIFICADO** (sin acceso a esa base) | `select indexname from pg_indexes where indexname in ('ae_coin_ledger_mp_payment_uk','ae_coin_ledger_spend_order_uk','ab_tx_mp_uniq')` |
| Misma cuenta de MercadoPago en LabelFlow / AE / AB | **NO DETERMINADO** — decide si los payment ids pueden colisionar | `GET https://api.mercadopago.com/users/me` con cada token (no pasar tokens) |
| Login de DAC: ¿cédula, mail o usuario? | **NO DETERMINADO** — `DAC_INTEGRATION_PLAN.md` lo marca bloqueante | Sólo Adrian |
| Formato/prefijo de guía DAC (¿identifica la cuenta?) | **NO DETERMINADO** — decide si `Label.dacGuia @unique` global es bug (F11) | Ver una guía de otra tienda que no sea Aura (las de Aura arrancan en `88214`) |
| Pasarela COD en Shopify (nombre) | **NO DETERMINADO** | Admin de Shopify de un cliente |
| Peso/dimensiones por defecto DAC | **NO DETERMINADO** | Módulo DAC / Adrian |
| Link del Excel actual | `autoenvia-dash/api/excel/template` genera la plantilla; no hay link público | Activar el módulo |
| Whop: negocio/company creada | **NO** — la cuenta es sólo personal (sidebar "Personal", saldo $0,00) | whop.com → "+" → crear company |
| Whop: pago `pay_om9KjtKtvUhBiU` | La URL es el retorno post-checkout; el detalle no aparece en la página pública | Whop → Settings → Purchases |
| Repos `autoenvia-dash` y `autoboost` sin remote git | Respaldados en `~/proyectos/_backups/` (tar.gz) | Crear repos privados en GitHub (Adrian) |
| `ALLOW_PUBLIC_SIGNUP=true` en Vercel después del merge de `feat/signup-publico` | **FALTA** (D23) — sin eso `/signup` se ve pero el POST responde 403 y el alta por Google cae en la pantalla de error de NextAuth. | `npx vercel env add ALLOW_PUBLIC_SIGNUP production --scope gurujk7s-projects` + redeploy. Sólo Adrian/orquestador. |
| Copy comercial del alta ("10 envíos gratis", "sin tarjeta", "cancelás cuando quieras") | **A CONFIRMAR por Adrian** — se sacó "valor $200 UYU" del `SignupForm` porque no hay dónde verificarlo; el 10 coincide con `Tenant.shipmentCredits @default(10)`. | Adrian |
| Exigir email verificado para entrar (`EMAIL_VERIFICATION_REQUIRED`) | **DECISIÓN de producto** (D24) — el login con contraseña no lo exige; el gate existe pero está apagado. Prenderlo sólo con `RESEND_API_KEY` viva y un alta de prueba que reciba el mail. | Vercel env vars, en ese orden |
| Qué ve un tenant sin cuenta DAC después de crear la cuenta | **DECISIÓN de producto** (D23) — `(dashboard)/layout.tsx` redirige a `/onboarding` hasta cargar Shopify **y** DAC, y sólo `onboarding/complete` activa el tenant. El self-serve termina ahí para quien no tiene DAC. | Adrian |
| Marca en el alta: LabelFlow vs AutoEnvía | **PARCIAL** — la metadata de `/signup` dice AutoEnvía; el logo del form, `/login`, `/verify-email`, el mail de verificación (`renderVerificationEmail`) y las páginas legales dicen LabelFlow. | Decisión de marca de Adrian; después es texto |
| Anti-abuso más fuerte en el alta (Turnstile, dominios desechables, rate limit en `/api/referrals/track`) | **NO HECHO** (D25/D26) — sin dependencias nuevas quedó rate limit por IP (IPv6 por /64) + tope global 40/h + honeypot + email normalizado (Redis, fail-open logueado). El tope global es un kill-switch: un script que rota IPs puede llenarlo y frenar altas reales una hora; eso sólo lo resuelve Turnstile. Segunda pasada (D26): el reenvío de verificación tiene 5/día por email + 20/h por IP, el honeypot responde 201 con ids falsos (indistinguible de un alta real) y no hay bono de referido si la IP de alta coincide con la del referidor. Sin rate limit todavía en `POST /api/referrals/track`. | Decidir según altas falsas después de abrir; skill `turnstile-spin` si se elige Turnstile |
| Circuit breaker en el worker: N fallos seguidos de `smartLogin` DAC para un tenant → `isActive=false` + aviso | **FALTA** (D26) — hoy un tenant activo con DAC inválido genera un job cada 15 min contra el bridge del Mac mini (`MAX_INFLIGHT` 3, compartido con clientes reales) para siempre: los créditos sólo bajan por envíos exitosos. `apps/worker/**` no se toca en `feat/signup-publico`. | Rama aparte sobre `apps/worker/src/jobs/process-orders.job.ts` + `scheduler.ts` |
| `emailVerified` para el comerciante que instala desde el App Store de Shopify | **RESUELTO por otro camino** (D26) — `provisionFromShopify` sigue sin setearlo (no se toca PR #3): el mail "elegí tu contraseña" de `/callback` es un token de reset y `password-reset/confirm` marca `emailVerified` al usarlo (control del buzón demostrado), así que al elegir contraseña queda verificado y puede activar. Si no usa ese mail no tiene contraseña: entra por "olvidé mi contraseña" (mismo camino) o por Google (`auth.ts` ya marca `emailVerified`). Queda por decidir si además se confía en el email del OAuth de Shopify para marcarlo al aprovisionar. | Decisión de Adrian (opcional); hoy no bloquea nada |
| Cuota de Resend del remitente `noreply@autoenvia.com` | **NO VERIFICADO** — la revisión cita de memoria 100/día y 3.000/mes para el plan gratis; con el tope global (≤ ~1.000 altas/día) seguiría por encima de un plan gratis. Ver el plan real antes de prender `ALLOW_PUBLIC_SIGNUP`. | resend.com → Settings → Plan (sólo Adrian) |
| QA del alta en preview de Vercel | **AVISO** — la cookie de sesión lleva `domain: '.autoenvia.com'` en producción (`lib/auth.ts:92`): en `*.vercel.app` el login por credenciales no persiste sesión y el paso login → onboarding "falla" aunque el código esté bien. Probar en el dominio real. | — |
| REST vs GraphQL: qué HTTP **y qué cuerpo** devuelve REST a la app pública nueva | **NO VERIFICADO** (D27, revisión 2026-09-01) — la doc no lo publica y `fetchShopInfo` tragaba el status. El selector `auto` conmuta REST→GraphQL sólo con **403 + cuerpo que mencione `REST Admin API`** (match positivo, patrón inferido), nunca con `required permission` ni `merchant approval`, y nunca para un tenant al que REST ya le respondió 2xx en el proceso. Si el texto real no matchea, un cliente con slug viejo que pase por Reconectar con token de la app pública ve un 403 claro en el runlog (no degrada en silencio). Los tenants del App Store no dependen de esto (GraphQL directo por igualdad exacta de slug). | `curl -s -w '\n%{http_code}' -H 'X-Shopify-Access-Token: <token de autoenvia-qa>' https://autoenvia-qa.myshopify.com/admin/api/2024-01/shop.json` (sin pegar el token en ningún chat). Ajustar `REST_403_FORBIDDEN_PATTERNS` en `apps/worker/src/shopify/mode.ts` al texto real y sumar el cuerpo al test `shopify-mode.test.ts`. Antes de prender `auto` con tenants reales: `SELECT id, slug, "shopifyStoreUrl" FROM "Tenant" WHERE slug LIKE 'shop-%';` y confirmar que cada fila del App Store cumple `slug = tenantSlugForShop(shopifyStoreUrl)` (las que no cumplen quedan en REST, que es lo correcto si su token es de custom app). |
| GraphQL: equivalencia del listado con REST `fulfillment_status=unfulfilled` | **NO VERIFICADO, riesgo acotado** (D27, revisión 2026-09-01) — ya NO se usa `fulfillment_status:` en el query (sus valores en 2026-07 no documentan si `unfulfilled` incluye partial/on_hold/scheduled/in_progress): se lista `financial_status:paid status:open` y se filtra del lado nuestro con la regla REST (null o partial). Lo que falta confirmar es que el set de ids coincida y cuántos crudos escanea una tienda real (tope 1000 con warn). | En `autoenvia-qa`: un pedido on_hold, uno parcial y uno preparado sin archivar; diff de ids REST (token de custom app) vs GraphQL, y mirar el log `Shopify GraphQL orders page cost` / el warn `scanned cap reached`. |
| GraphQL: costo real de la página inicial (`first: 25 / lineItemsFirst: 50`) y `status` sin filtro en `getRecentOrders` | **NO VERIFICADO** (D27) — el cliente achica la página solo ante `MAX_COST_EXCEEDED` y loguea `requestedQueryCost/actualQueryCost` por página. `getRecentOrders` (modo TEST) va sin `query`: no está documentado si eso incluye cerrados/cancelados como `status=any`. | Primer ciclo en `autoenvia-qa` con log en INFO; para TEST, un pedido cerrado y ver si aparece. |
| Datos protegidos de cliente (nombre, dirección, email, teléfono) para la app pública | **BLOQUEANTE de producto** (D27) — sin la solicitud aprobada en el Partner Dashboard el Admin API devuelve esos campos en null con HTTP 200 + `errors[]`. Desde la revisión 2026-09-01 el worker lo detecta en `getUnfulfilledOrders`/`getRecentOrders` y aborta el tenant con `ShopifyProtectedDataError` (un solo error accionable en el runlog, cero notas escritas en los pedidos) en vez de escribir "LabelFlow ERROR: No shipping address" en cada pedido cada ciclo. En tiendas de desarrollo (autoenvia-qa) funciona sin aprobación, así que el QA no lo va a mostrar: el primer tenant real va a quedar en FAILED con ese mensaje hasta que se apruebe. | Partners → app AutoEnvía → API access → Protected customer data access → pedir los 4 (ya estaba en esta tabla como "Acceso a datos protegidos"). Sólo Adrian. |
| `read_customers` no está en los scopes de la app pública | **DECISIÓN de Adrian** (D27) — el adaptador entrega `customer: null`: `resolveOrderPhone` pierde `customer.phone` y `customer.default_address.phone` (prioridades 3 y 5; quedan shipping, billing y `order.phone`) y las reglas de envío por `customer.tags` no matchean en tenants App Store. Agregarlo toca `shopify.app.toml`, `REQUIRED_SCOPES` en `lib/shopify-oauth.ts` y sus tests, re-autoriza cada tienda instalada y probablemente sube el nivel de datos protegidos. | Adrian decide; después es una rama chica (`customer { legacyResourceId tags defaultAddress { phone } defaultPhoneNumber { phoneNumber } }` — `customer.phone/email` están deprecados en 2026-07). |
| Webhooks duplicados: `shopify.app.toml` declara suscripciones app-scoped para `orders/paid` y `app/uninstalled` | **NO VERIFICADO** (D27) — `webhookSubscriptions` devuelve SÓLO las shop-scoped; si `shopify app deploy` aplicó las del TOML, `registerShopifyWebhooks` crea una segunda y cada pedido pago llega dos veces al receptor. | Partner Dashboard → app → versión activa → Webhooks. Si las app-scoped están vivas, `registerShopifyWebhooks` en el callback pasa a ser verificación (no-op). |
| Rutas de `apps/web` que siguen en REST (`shopify-scopes`, `products/scan`, `fulfill-retry`, `clean-notes`, `shopify-reconcile`, `shopify-pending`, `recover-setup` + `recover/sync`, `wms-items-backfill`; `settings`/`test-shopify` quedan REST a propósito) | **FUERA DE ALCANCE, documentado en D28** — para un tenant `shop-*` degradan con error claro y sin crash (verificado leyendo cada ruta): scopes → 502, scan → sin catálogo, fulfill-retry → error de Shopify en la respuesta, contador de pendientes vacío, reconcile/backfill atrapados por sus llamadores. | Una rama chica por ruta sobre `apps/web/lib/shopify-graphql.ts` (D28 lista la query/mutación equivalente de cada una). |
| `SHOPIFY_API_MODE` en Render (worker) | **OPCIONAL** (D27) — default `auto`. `rest` deja todo como antes sin deploy (ni se cargan los módulos GraphQL); `graphql` fuerza GraphQL para todos (sólo tiene sentido cuando todos los tenants tengan token de la app pública). | Render → worker → Environment. Sólo si hace falta. |

## Onboarding de 6 pasos (D33, rama `feat/selfserve-v1`)

Lo que el código ya hace: `/onboarding` es un server component que deriva el paso de la base (`lib/onboarding-state.ts`), el gate del dashboard acepta Shopify **o** Dashboard con Excel, `POST /api/v1/jobs` encola por tipo de tienda, `install?next=` vuelve al wizard, y `PUT/GET /api/v1/settings` exponen `codEnabled`. Lo que queda:

| Ítem | Estado | Dónde / cómo |
|---|---|---|
| **Columna `Tenant.codEnabled` en prod** (H7) | **VERIFICADO 2026-09-02, está**: `information_schema.columns` sobre `DIRECT_URL` devuelve `Tenant.codEnabled boolean DEFAULT false` y `Label.codAmount integer`. El GET de `/api/v1/settings`, `GET /api/v1/onboarding/state` y `/onboarding` pueden seleccionarla. `scripts/sql/cod-contrarreembolso.sql` quedó marcado como aplicado. | Nada. |
| **Worker de Render con el código del contrareembolso** | **NO VERIFICADO desde acá** (el MCP de Render pide elegir workspace y eso lo hace Adrian; autoDeploy del worker está OFF). Sin `df13204` (`process-orders.job.ts:805-810` lee `tenant.codEnabled`) el toggle mentiría. Por eso el contrareembolso vive detrás de **`COD_FEATURE_ENABLED`** (`lib/cod-feature.ts`, default apagada): GET devuelve `codAvailable: false`, PUT rechaza `codEnabled: true` con 422, el bloque se muestra como "Próximamente". | Render → worker → último deploy → commit ≥ `df13204` (2026-09-01). Si no, "Manual deploy" desde main. Después `npx vercel env add COD_FEATURE_ENABLED production --scope gurujk7s-projects` con `true`. |
| **Límite de pedidos con Dashboard con Excel** | `process-dashboard-orders.job.ts` trae hasta `DASHBOARD_FETCH_LIMIT=100` confirmados y recorta sólo por saldo; no lee el RunLog `maxOrdersOverride` (`process-orders.job.ts:148-183` sí). Desde la revisión 2026-09-02 `POST /api/v1/jobs` responde 422 si el tenant es de Excel y pide `maxOrders`/`testMode`, y el Dashboard no ofrece los chips 1/3/5/10/20 (botón "Ejecutar pedidos confirmados", body `{}`). `POST /api/v1/control/run` no está afectado: siempre encola `PROCESS_ORDERS` (Shopify), que en un tenant de Excel falla sin despachar. | Worker, otro turno: leer el override en el job de Dashboard igual que en el de Shopify y sacar el 422. |
| Tenants legacy con `onboardingComplete=false` e `isActive=false` | Con el gate nuevo (H4) van al wizard (2 clicks: Continuar → Activar) y al completar quedan `isActive=true`. Si alguno estaba pausado a propósito, se reactiva. | Listar antes del deploy: `SELECT id, slug FROM "Tenant" WHERE "onboardingComplete"=false AND "isActive"=false AND "dacUsername" IS NOT NULL;` |
| Configuración > Parámetros (Área D, §6.3) | **NO HECHO en este turno** — `ParametrosForm` y `ModoProcesamiento` existen en `settings/_components/` y los usa el wizard (pasos 4 y 5); Configuración sigue con sus secciones viejas ("Regla de pago", "Procesamiento de pedidos", slots) más una tarjeta "Asistente de configuración" con links a `/onboarding?step=N`. Falta montar `ParametrosForm` en `#parametros` y esconder slots / reparto propio / impresión / API key / Test DAC para no-admin. | Área D. |
| Mensaje del job al activar | Si "Activar y procesar ahora" activa pero el job no se encola (sin saldo, uno en curso), el wizard muestra el motivo 2,5 s y va al dashboard. El dashboard no recibe el mensaje (no tiene mecanismo de aviso por query). | Área F, si se quiere un banner. |
| `POST /api/v1/jobs` responde HTTP 200 con `meta.status: 202` | Preexistente: `apiSuccess(data, { status: 202 })` mete el status en `meta`, no en la respuesta. El wizard y el dashboard miran `res.ok`, así que funciona; el test nuevo fija el 200 real. | Cambiar `apiSuccess` para aceptar init es otro turno (toca todas las rutas). |
| Parámetros de envío para Dashboard con Excel | **NO SOPORTADO en el worker** — `apps/worker/src/jobs/process-dashboard-orders.job.ts` llama `createShipment(page, order, 'DESTINATARIO', …)` sin `codAmount` ni `skuInObservations`, y no lee `paymentThreshold`, `shippingRules`, `allowedProductTypes`, `emailHost` ni `orderSortDirection`. Desde la revisión 2026-09-02 la UI lo dice (un aviso único en el paso 4 y en Configuración) en vez de ofrecer toggles que no hacen nada. Si un cliente de Dashboard necesita contrareembolso o SKU, hay que darle soporte en ese job (mismo motor que el de Shopify; `shipment.ts` es intocable, el cambio va en el job). | Otro turno, en el worker. |
| `ADMIN_EMAILS` en Vercel (rol admin, D32) | **CARGADA en producción** con `admin@autoenvia.com` y el gmail de Adrian (Adrian, 2026-09-02). Sin la var nadie es admin (menú de usuario, 404 en /control, /orders, /reports y /admin). Desde la revisión 2026-09-02 el admin ve en Control **todas las tiendas activas de todos los clientes**, agrupadas por dueño, y puede ejecutar/reintentar/ver pedidos sobre ellas (`lib/control-scope.ts`); Eliminar sigue siendo sólo del dueño. Documentada en `.env.example`. | Sumar otro admin = editar la var en Vercel + redeploy (se lee en runtime, pero Vercel no re-inyecta env sin deploy). |
| Webhook instantáneo con token manual (H9) | El copy del paso 2 y del paso 5 lo dice ("con tu forma de conexión, el aviso instantáneo no está disponible" se muestra cuando `store.kind !== 'shopify'`; con Shopify por token manual no lo sabemos y no se avisa). | Botón "Reconectar con Shopify" en Configuración, otro turno. |

## Whop como segundo riel de packs (D30/D34, rama `feat/selfserve-v1`)

Lo que el código ya hace: `GET /api/credit-packs/whop-checkout?pack=` crea la compra PENDING y redirige al link del pack; `POST /api/webhooks/whop` verifica la firma (Standard Webhooks, fail-closed), deduplica, resuelve la compra y acredita con la misma función que MercadoPago. Lo que falta es **de Adrian, en Whop y en Vercel**:

| Ítem | Estado | Dónde / cómo |
|---|---|---|
| Company en Whop | **FALTA** — la cuenta es personal, saldo USD 0, sin negocio (visto 2026-09-01, `docs/PAGOS.md` §1) | whop.com → crear company. Sin esto no hay producto ni webhooks. |
| Un **producto** con **un plan (one-time) por pack** que se quiera vender por Whop | **FALTA — y desde D35 los precios en USD ya están decididos, ver la sección "Planes de Whop" más abajo.** El webhook exige que el plan pagado sea el del pack (`WHOP_PLAN_IDS`) y, opcionalmente, un piso en USD por plan (`minUsd`). | Whop dashboard → Products → Pricing → "One-time". Copiar el **link de checkout** de cada plan. |
| URL de retorno de cada plan | **FALTA** — poner `https://autoenvia.com/settings/billing?whop=return`. Whop le agrega `?payment_id=pay_…&checkout_status=success` (PAGOS.md §2); la página muestra "volviste de Whop" y **no acredita**: acredita el webhook. | En la config del plan (redirect / "after purchase" URL). |
| Env `WHOP_CHECKOUT_URLS` en Vercel (producción) | **FALTA** — JSON `{packId: url}` con los links del punto anterior. Sin la var los botones de Whop no existen y el endpoint da 404. JSON inválido = igual que ausente (un `console.error`). Sólo se aceptan URLs `https://`. | `npx vercel env add WHOP_CHECKOUT_URLS production --scope gurujk7s-projects`. No es secreto pero no es `NEXT_PUBLIC_`: las URLs no viajan al cliente. |
| Env `WHOP_PLAN_IDS` en Vercel (producción) | **FALTA y es OBLIGATORIA para acreditar** (revisión 2026-09-02) — JSON `{packId: plan_id}` o `{packId: {"planId": "plan_…", "minUsd": N}}`. Los links de checkout de Whop son públicos y no llevan metadata: sin esto, cualquiera podía pagar el plan barato (pack_10) teniendo una compra PENDING de pack_1000 y el webhook acreditaba 1.000 envíos. Ahora el webhook exige que el plan/producto del evento sea el asignado al `packId` de la compra; con `minUsd` además exige moneda USD y monto ≥ piso. Sin la var, con JSON roto, sin plan en el payload o con plan distinto → 200 `flagged` + `[whop] plan no coincide con la compra` (con `reason`), **0 envíos**. | `npx vercel env add WHOP_PLAN_IDS production --scope gurujk7s-projects` con los `plan_…` de cada plan creado. Setearla ANTES o junto con `WHOP_CHECKOUT_URLS`. |
| Nombre del campo del plan y unidad del monto en el payload de Whop | **INFERIDO, no verificado** — el webhook acepta `data.plan_id`, `data.plan.id`, `data.product_id` y `data.product.id` (alcanza con que el configurado sea uno de ellos), y el monto en `data.final_amount` / `data.amount` / `data.total` con `data.currency`. No está verificado si `final_amount` viene en dólares (5.00) o en centavos (500): si viene en centavos, `minUsd` marcaría como `amount_below_min`… nunca acredita de más. | Primer pago de prueba: mirar el log `[whop] plan no coincide` (trae `reason` y `payloadPlanIds`, sin cuerpo) y ajustar `payloadPlanIds()` / `asNumber(...)` en `app/api/webhooks/whop/route.ts`. Poner `minUsd` recién después de confirmar la unidad. |
| Webhook en Whop → `https://autoenvia.com/api/webhooks/whop`, eventos `payment.succeeded` (obligatorio) + `payment.refunded` / `refund.created` / `dispute.created` | **FALTA** | Whop dashboard → Developer → Webhooks. Copiar el signing secret (`whsec_…`). |
| Env `WHOP_WEBHOOK_SECRET` en Vercel | **FALTA** — sin ella el webhook responde **503** a todo (fail-closed, igual que MP). Acepta el secret con prefijo `whsec_` (base64) o plano. | `npx vercel env add WHOP_WEBHOOK_SECRET production --scope gurujk7s-projects`. Secreto: no pegarlo en chats ni notas. |
| **Shape del payload de Whop** (`type`/`action`, `data.id = pay_…`, `data.user.email`, `data.metadata`) | **INFERIDO de memoria, no verificado contra un evento real**. El webhook acepta `type`, `action` o `event`, `payment.succeeded` y `payment_succeeded`, y busca el email en `data.user.email` / `data.user_email` / `data.email`. Los tests fijan ese supuesto. | Primer pago de prueba (plan de USD 1 o cupón 100%): mirar el log de Vercel (`[whop] …`, sin cuerpo) y, si algo no cuadra, ajustar los `??` de `app/api/webhooks/whop/route.ts` (`handlePaymentSucceeded` / `resolvePurchaseId`). |
| Metadata en links estáticos de checkout | **NO VERIFICADO** que un link acepte `?metadata[purchaseId]=`. Por eso la compra se resuelve por `metadata.purchaseId` → `metadata.userId` → **email del pago + única compra PENDING de Whop del usuario en 24 h**. Si el email de Whop no es el de AutoEnvía, o hay 0 ó 2+ PENDING, el webhook responde 200 `flagged` y **no acredita**: queda `[whop] compra no resuelta {webhookId, paymentId, candidates}` en el log para acreditar a mano. | Camino para cerrarlo: checkout configurations por API con `metadata { purchaseId }` (PAGOS.md §3.1) en vez de links fijos. Otro turno. |
| Acreditación manual de un pago `flagged` | Procedimiento: buscar el `CreditPurchase` PENDING con `mpExternalRef LIKE 'whop|%'` del usuario, y llamar `settlePaidPurchase({ purchaseId, externalPaymentId: 'whop:<pay_id>', rail: 'whop' })` (script o SQL equivalente: `status='PAID'`, `mpPaymentId='whop:<pay_id>'`, `paidAt=now()` + `shipmentCredits`/`creditsPurchased` del **holder**). El `mpPaymentId @unique` impide acreditar dos veces el mismo pago. | Sólo Adrian, con el `paymentId` del log. |
| Copy de moneda en la UI | **RESUELTO por D35** — la página muestra el precio en dólares (que es la denominación) y aclara que MercadoPago cobra en pesos al tipo del día, con el tipo a la vista. | — |

### Planes de Whop con los precios de D35 (para que Adrian los cree)

Un plan **one-time** por paquete. El precio es exactamente el total del paquete en dólares —
no hay FX que aplicar, Whop cobra en USD. `minUsd` se pone recién después de confirmar si
`final_amount` viene en dólares o en centavos (fila de arriba); mientras tanto, dejarlo sin `minUsd`.

| Plan (para `WHOP_PLAN_IDS`) | Envíos | Precio del plan (USD) | USD por envío | ¿Vale la comisión? |
|---|---|---|---|---|
| `pack_10` | 10 | **5,00** | 0,50 | **No** — breakeven ~USD 14 (PAGOS.md §1). No crearlo. |
| `pack_50` | 50 | **21,00** | 0,42 | Justo en el límite. Opcional. |
| `pack_100` | 100 | **37,00** | 0,37 | Sí |
| `pack_250` | 250 | **75,00** | 0,30 | Sí |
| `pack_500` | 500 | **125,00** | 0,25 | Sí |
| `pack_1000` | 1000 | **175,00** | 0,175 | Sí |
| `pack_2500` | 2500 | **350,00** | 0,14 | Sí — **nuevo en D35** |
| `pack_5000` | 5000 | **550,00** | 0,11 | Sí — **nuevo en D35** |

🔴 Si algún día cambia un precio de la escalera, estos planes quedan desactualizados **en Whop, no en
el código**: ningún test puede verificar lo que hay del otro lado. El `minUsd` de `WHOP_PLAN_IDS` es
la única defensa contra cobrar de menos, y sólo funciona si Adrian lo actualiza junto con el plan.

## Precios en dólares (D35)

| Ítem | Estado | Dónde / cómo |
|---|---|---|
| ~~FIRMA PENDIENTE: el escalón de 1.000 envíos~~ → **FIRMADO 2026-09-02: `180n` → `175n`** | **RESUELTO.** Adrian eligió la opción (b): el escalón de 1.000 pasa a **USD 0,175** = 7/40 exacto, así que queda en **7,00 UYU** y ningún cliente actual paga más. Aplicado en `apps/web/lib/pricing.ts` **y** `apps/worker/src/billing/tiers.ts` (el test de sincronía obliga a los dos); D35 actualizada. Se descartaron (a) dejar 0,18 y avisar, y (c) 170n. | **Queda una consecuencia operativa, no una tarea de código:** `maxRateWithoutIncreaseMilli()` pasó de 38,888 a **40,000**, o sea el tipo base exacto. **Ya no hay margen: cualquier `USD_UYU_RATE` > 40 encarece precios viejos** y escribe una ALERTA DE PRECIO en los logs del checkout. Tenerlo presente al fijar la env (fila de abajo). |
| Valor real de `USD_UYU_RATE` en Vercel | **FALTA** — sin la var se cobra al tipo base 40. Ver la fila "Tipo de cambio UYU/USD" arriba. | Sólo Adrian. |
| Avisar a los clientes actuales del cambio de moneda | **NO HECHO** — el precio pasa a estar en dólares: si el peso se devalúa, lo que pagan en pesos sube sin que cambie la escalera. Es un cambio de condiciones comercial, no técnico. | Decisión y comunicación de Adrian (WhatsApp / mail). |
| Landing con la calculadora de precios | **OTRA RAMA** (`feat/landing-v2`, PR #13) — no se tocó ni `app/page.tsx` ni sus componentes. La fuente única quedó exportada y lista para consumir: `PRICING_TIERS`, `quoteUsd()`, `periodTotalUsdMilli()`, `nextTierHint()`, `formatUsdMilli()` y `formatUsdUnitMilli()` de `apps/web/lib/pricing.ts`. Todas puras, sin env ni DB, usables en un componente de cliente. **El selector de moneda USD/UYU también quedó listo y sin usar allá** (`apps/web/app/_components/CurrencyToggle.tsx`), con sus tests. | **Las líneas exactas para la landing.** Import: `import { CurrencyToggle, useCurrency } from '@/app/_components/CurrencyToggle';` y `import { currencyNote, formatTotalPrice, formatUnitPrice } from '@/lib/pricing';`. Estado: `const [currency, setCurrency] = useCurrency();`. Render del control: `<CurrencyToggle value={currency} onChange={setCurrency} label="Ver los precios en" />`. Montos: `formatUnitPrice(BigInt(unitUsdMilli), { currency, rateMilli })` y `formatTotalPrice(BigInt(totalUsdMilli), { currency, rateMilli })`. Aclaración legal debajo de los precios: `{currencyNote(currency, rateLabel)}`. 🔴 **La trampa: `rateMilli` y `rateLabel` NO se pueden calcular en la landing.** `USD_UYU_RATE` es env de servidor y en el bundle del navegador `process.env` viene vacío, así que `getUsdUyuRateMilli()` caería al tipo base sin avisar y la landing publicaría pesos distintos de los que cobra el checkout. Hay que pasarlos desde el server component que envuelve la calculadora: `const rateMilli = getUsdUyuRateMilli(); const rateLabel = formatRate(rateMilli);` y bajarlos por props (es lo mismo que hace `/api/credit-packs/me` para el dashboard). 🔴 Y la landing comparte la clave de localStorage con el dashboard (`autoenvia.currency`), así que la moneda elegida en una pantalla se respeta en la otra: es deseado, pero si allá se cambia la clave se rompe. |
| 🔴 **Vender `pack_2500` / `pack_5000` en autoservicio: FRENADO hasta que Adrian firme** | **BLOQUEADO por la revisión del 2026-09-02.** Un paquete cobra `periodTotalUsdMilli(tamaño del pack)` —el precio del escalón aplicado al TAMAÑO DEL PACK, no al volumen mensual real—, los envíos se acreditan 1:1 y **no vencen**: comprar grande compra un precio de escalón alto **para siempre** sin tener nunca ese volumen. Medido: un cliente de **60 envíos/mes** (escalón 0,42 → USD 25,20 el mes) compra `pack_5000` por **USD 550** y lo consume en **83 meses**; a su volumen real habría pagado **USD 2.100** → **-73,8 %** de ingreso. Y dos clientes del mismo volumen pagan **0,11 vs 0,42 (3,8×)** según qué paquete compraron. El mecanismo ya existía y topea en **65 %** (`pack_1000`, 0,175); estos dos lo llevan a **78 %**: la rama duplicaría el techo del agujero. Mientras tanto la escalera de D35 se sigue mostrando **completa** (es el precio por volumen mensual, que es lo que D35 decidió) y sólo se gatea qué se puede **comprar solo**; arriba de 1.000 la UI manda a cotizar a medida. Fijado por `lib/__tests__/credit-packs-selfserve.test.ts`. | **Decisión de Adrian.** Para habilitarlos tal cual están: `npx vercel env add ENABLE_LARGE_CREDIT_PACKS production --scope gurujk7s-projects` con `true`. Antes de eso conviene elegir una de: (a) venderlos sólo con contrato (lo que hay hoy); (b) darles vencimiento por período — necesita columna nueva, o sea migración; (c) topear el pack comprable al escalón que justifique el histórico de envíos del tenant. |
| Planes de Whop para los dos escalones nuevos | **FALTA — y no urge**: mientras `ENABLE_LARGE_CREDIT_PACKS` esté apagada esos packs no se venden por ningún riel (`getPack` los rechaza y el endpoint de Whop pasa por ahí). Cuando se habiliten, `pack_2500` (USD 350) y `pack_5000` (USD 550) necesitan su plan. Ver la tabla de arriba. | Adrian, en Whop. |
| 🔴 **Dos tipos de cambio en el mismo producto — hoy inerte, se activa con PR #4** | **ANOTADO, no arreglado (no se puede arreglar todavía).** El checkout convierte a pesos con `USD_UYU_RATE` (movible); el ledger del worker tarifa con **40 fijo** (`BASE_USD_UYU_RATE_MILLI`, y está bien que sea fijo: una env movible reescribiría períodos ya liquidados y reventaría `assertPeriodInvariant`). **Hoy no hay fuga**: el saldo vivo se cuenta en ENVÍOS (`Tenant.shipmentCredits`, 1:1) y el ledger en pesos está en sombra, sin camino de depósito. Pero el docblock del worker declara el modelo destino ("el monedero se denomina en PLATA... Comprar = depositar plata") y **PR #4 (`feat/wallet-ledger`) es el que lo va a cablear**. Simulado conectando el depósito: a tipo 44 `pack_5000` deposita 24.200 UYU y compra **5.500** envíos (+10 %, pierde la empresa); a tipo 36 deposita 19.800 y compra **3.535** (**-29,3 %**, pierde el cliente — mucho peor que el -10 % que sugiere 36/40, porque cae justo debajo del techo de la meseta 3.930-5.000 y se cuelga ahí), y `pack_1000` a 36 da **630** envíos (**-37,0 %**). | Cuando se cablee el depósito, **una de las dos**: (a) el asiento del depósito guarda el tipo con el que se convirtió ESA compra y el saldo se consume contra ese tipo; o (b) el ledger se denomina en milésimos de USD y convierte sólo al mostrar. Lo que **no** se puede es que el worker lea `USD_UYU_RATE`. Hay un test que falla en cuanto aparezca un camino de depósito y apunta a esta decisión: `apps/worker/src/__tests__/billing-fx-deposit-tripwire.test.ts`. |
| Precio de los bots / AutoBoost en la nueva denominación | **NO REVISADO** — D35 sólo tocó el precio de los envíos. Si AutoBoost cobra en pesos contra el mismo saldo, queda un tarifario en pesos conviviendo con uno en dólares. | Fuera del alcance de esta rama; revisar con Adrian. |
