{
  "name": "labelflow",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:worker": "npm run dev --workspace=apps/worker",
    "build": "npm run check-schemas && npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "check-schemas": "bash scripts/check-schemas.sh",
    "sync-schemas": "cp apps/web/prisma/schema.prisma apps/worker/prisma/schema.prisma && echo '[sync-schemas] copied web → worker'",
    "db:generate": "npm run sync-schemas && npx prisma generate --schema=apps/web/prisma/schema.prisma",
    "db:push": "npx prisma db push --schema=apps/web/prisma/schema.prisma",
    "db:migrate": "npx prisma migrate dev --schema=apps/web/prisma/schema.prisma",
    "db:studio": "npx prisma studio --schema=apps/web/prisma/schema.prisma"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
